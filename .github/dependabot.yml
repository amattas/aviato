version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
  # Keep the docs-site scaffold's dependency set fresh — consumers copy its
  # package.json + lockfile verbatim.
  - package-ecosystem: npm
    directory: /starter/docs-site
    schedule:
      interval: weekly
