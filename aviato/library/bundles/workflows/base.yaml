name: base
# Shared graph-owned automation. Release and deployment variants are selected by
# profile bundles; drift is an explicit owner rather than a scaffold side effect.
pipelines:
  - security-baseline
  - common-lint
  - drift-automation
