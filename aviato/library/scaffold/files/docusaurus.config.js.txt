// Aviato docs baseline (§13.3): multi-version, local offline search, docs-first.
// Seed-once: Aviato writes this when absent; you then own it. Language API md/mdx
// (pdoc / TypeDoc / narrative) is emitted into docs/ by the docs build step (§12).
// @ts-check
const config = {
  title: 'Documentation',
  url: 'https://example.github.io',
  baseUrl: '/',
  onBrokenLinks: 'throw',
  themeConfig: {
    colorMode: { defaultMode: 'light', respectPrefersColorScheme: true },
    navbar: { items: [{ type: 'docsVersionDropdown', position: 'right' }] },
  },
  presets: [
    ['@docusaurus/preset-classic', { docs: { routeBasePath: '/', sidebarPath: './sidebars.js' }, blog: false }],
  ],
  themes: [
    // Local offline search — no external service, no API key (§13.3 / §11.2).
    ['@easyops-cn/docusaurus-search-local', { hashed: true, indexDocs: true }],
  ],
};
module.exports = config;
