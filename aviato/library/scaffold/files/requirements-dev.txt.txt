# Dev tools the Aviato container-service CI invokes (ruff lint, mypy type-check, pytest).
# Pinned to a floor; your runtime deps live in requirements.txt (used by the Dockerfile).
pytest>=8.0
pytest-cov>=5.0
mypy>=1.8
ruff>=0.8.0
