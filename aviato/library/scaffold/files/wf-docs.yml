name: Aviato Docs

# Multi-version Docusaurus docs publish (§13.3), opt-in via docs: true.
on:
  push:
    tags: ["*"]

permissions:
  contents: read
  pages: write
  id-token: write
  actions: read

jobs:
  docs:
    if: ${{ github.ref_type == 'tag' }}
    uses: amattas/aviato/.github/workflows/reusable-docs-pages.yml@{{ aviato-ref }}
