{
  "name": "{{ project-name }}",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=18.0"
  },
  "scripts": {
    "lint": "eslint .",
    "format:check": "prettier --check .",
    "test": "vitest run --coverage",
    "build": "echo 'No build configured. Add a bundler (e.g. esbuild) and set run-build for the CI gate.' && exit 1"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "eslint-plugin-security": "^3.0.0",
    "prettier": "^3.0.0",
    "vitest": "^2.0.0"
  }
}
