"""Aviato agnostic core engine.

This package knows how to resolve, compose, scaffold, diagnose, and reconcile
conventions. It contains no language- or deployment-specific logic and has no
import edge into any plug-in tree (enforced by :mod:`aviato.core.selfcheck`).
"""
